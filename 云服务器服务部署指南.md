# Koukou 云服务器部署指南（最简版）

服务器信息：

```text
云服务器：阿里云 ECS
实例名：zzj1
实例 ID：i-bp1fl4qtawqmt6l4vldn
系统：Ubuntu 22.04 64 位
公网 IP：120.26.247.39
私网 IP：172.30.151.177
域名：zzj.abrdns.com
服务端目录：/opt/koukou/koukou-server
API 地址：https://zzj.abrdns.com
WebSocket 地址：wss://zzj.abrdns.com/ws
证书方式：Let's Encrypt DNS-01 手动验证
证书到期：2026-08-10
当前状态：HTTPS 已通
```

## 1. 阿里云安全组

在阿里云控制台添加入方向规则：

```text
HTTP   TCP  80   0.0.0.0/0
HTTPS  TCP  443  0.0.0.0/0
SSH    TCP  22   你的IP/32
```

如果你不确定自己的公网 IP，SSH 可以临时用：

```text
SSH    TCP  22   0.0.0.0/0
```

不要开放：

```text
8080
3306
6379
```

原因：

```text
8080  只给 Nginx 本机反代
3306  MySQL 只允许本机访问
6379  Redis 只允许本机访问
```

## 2. 登录服务器

```bash
ssh root@120.26.247.39
```

## 3. 基础环境

```bash
sudo apt update
sudo apt install -y curl git nginx mysql-server redis-server ufw ca-certificates build-essential certbot python3-certbot-nginx

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm i -g pm2

node -v
npm -v
pm2 -v
```

## 4. 系统防火墙

```bash
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw --force enable
sudo ufw status
```

## 5. MySQL 初始化

测试环境统一使用密码 `123456`：

```bash
export MYSQL_ROOT_PASSWORD='123456'
export MYSQL_APP_PASSWORD='123456'
```

初始化数据库：

```bash
sudo mysql <<SQL
ALTER USER 'root'@'localhost' IDENTIFIED WITH caching_sha2_password BY '${MYSQL_ROOT_PASSWORD}';
CREATE DATABASE IF NOT EXISTS koukou_im
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'koukou'@'localhost' IDENTIFIED WITH caching_sha2_password BY '${MYSQL_APP_PASSWORD}';
ALTER USER 'koukou'@'localhost' IDENTIFIED WITH caching_sha2_password BY '${MYSQL_APP_PASSWORD}';
GRANT ALL PRIVILEGES ON koukou_im.* TO 'koukou'@'localhost';
FLUSH PRIVILEGES;
SQL
```

## 6. Redis 初始化

```bash
export REDIS_PASSWORD='123456'

sudo cp /etc/redis/redis.conf /etc/redis/redis.conf.bak.$(date +%F-%H%M%S)
sudo sed -i 's/^bind .*/bind 127.0.0.1/' /etc/redis/redis.conf
sudo sed -i 's/^protected-mode .*/protected-mode yes/' /etc/redis/redis.conf
sudo sed -i 's/^supervised .*/supervised systemd/' /etc/redis/redis.conf
sudo sed -i '/^[[:space:]]*requirepass[[:space:]]/d' /etc/redis/redis.conf
echo "requirepass ${REDIS_PASSWORD}" | sudo tee -a /etc/redis/redis.conf

sudo systemctl daemon-reload
sudo systemctl enable --now redis-server
sudo systemctl restart redis-server
redis-cli -a "${REDIS_PASSWORD}" ping
```

最后应输出：

```text
PONG
```

## 7. 拉取代码

```bash
cd /opt
rm -rf koukou
git clone https://github.com/tankingTC/koukou.git koukou
cd /opt/koukou/koukou-server
npm install
```

导入表结构：

```bash
mysql -u koukou -p koukou_im < /opt/koukou/koukou-server/sql/init.sql
```

输入 `MYSQL_APP_PASSWORD`。

## 8. 写入 .env

如果当前 shell 里还有 `MYSQL_APP_PASSWORD`，直接执行：

```bash
cd /opt/koukou/koukou-server

cat > .env <<EOF
PORT=8080
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=koukou
MYSQL_PASSWORD=${MYSQL_APP_PASSWORD}
MYSQL_DB=koukou_im
JWT_SECRET=$(openssl rand -hex 32)
DEV_AUTH_ALLOW_USER_ID=true
EOF

grep -v 'PASSWORD\|SECRET' .env
```

如果变量没了，先重新设置：

```bash
export MYSQL_APP_PASSWORD='123456'
```

## 9. 启动服务

```bash
mkdir -p /var/log/koukou-server

pm2 delete koukou-server || true
cd /opt/koukou/koukou-server
pm2 start ecosystem.config.js --env production --update-env
pm2 save
pm2 startup systemd
```

`pm2 startup systemd` 如果输出一行 `sudo env PATH=...`，复制那一行再执行一次。

检查：

```bash
sleep 2
pm2 status
curl http://127.0.0.1:8080/health
```

正常返回：

```json
{"ok":true,"service":"koukou-server","time":...}
```

## 10. 配置 Nginx

```bash
sudo rm -f /etc/nginx/sites-enabled/default

sudo tee /etc/nginx/sites-available/koukou-api >/dev/null <<'NGINX'
server {
    listen 80;
    server_name zzj.abrdns.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ws {
        proxy_pass http://127.0.0.1:8080/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
NGINX

sudo ln -sf /etc/nginx/sites-available/koukou-api /etc/nginx/sites-enabled/koukou-api
sudo nginx -t
sudo systemctl restart nginx
```

检查 HTTP：

```bash
curl http://127.0.0.1/health
curl http://zzj.abrdns.com/health
```

如果 `127.0.0.1` 正常但域名超时，回到第 1 步检查阿里云安全组 `80`。

## 11. 申请 HTTPS 证书

最省心方式：临时停止 Nginx，让 Certbot 自己监听 80 端口完成验证。

```bash
sudo systemctl stop nginx
sudo certbot certonly --standalone --preferred-challenges http -d zzj.abrdns.com
```

如果证书申请成功，直接写入 HTTPS/WSS 配置：

```bash
sudo tee /etc/nginx/sites-available/koukou-api >/dev/null <<'NGINX'
server {
    listen 80;
    server_name zzj.abrdns.com;

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name zzj.abrdns.com;

    ssl_certificate /etc/letsencrypt/live/zzj.abrdns.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zzj.abrdns.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ws {
        proxy_pass http://127.0.0.1:8080/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
NGINX

sudo nginx -t
sudo systemctl start nginx
sudo systemctl reload nginx
curl https://zzj.abrdns.com/health
```

如果 standalone 模式提示 80 端口被占用：

```bash
sudo ss -ltnp | grep ':80'
sudo systemctl stop nginx
sudo certbot certonly --standalone --preferred-challenges http -d zzj.abrdns.com
```

下面是 Nginx webroot 方式，只有在 standalone 不方便时再用。

当前 HTTP 已通时应满足：

```bash
curl -i http://127.0.0.1/.well-known/acme-challenge/ping
curl -i http://zzj.abrdns.com/.well-known/acme-challenge/ping
curl -i http://zzj.abrdns.com/health
```

都返回 `HTTP/1.1 200 OK`。

如果 Certbot 报 `403` 或验证内容异常：

```text
Invalid response from http://zzj.abrdns.com/.well-known/acme-challenge/...: 403
```

说明 Nginx 还没有正确放行 `/.well-known/acme-challenge/`。直接用下面这组最小配置修复。

```bash
# 1. 关闭所有已启用站点，避免其他 server/location 抢请求
sudo mkdir -p /etc/nginx/sites-disabled
sudo find /etc/nginx/sites-enabled -maxdepth 1 -type l -exec mv {} /etc/nginx/sites-disabled/ \; 2>/dev/null || true

# 2. 准备 ACME 验证目录和测试文件
sudo mkdir -p /var/www/certbot/.well-known/acme-challenge
echo ok | sudo tee /var/www/certbot/.well-known/acme-challenge/ping >/dev/null
sudo chmod -R 755 /var/www/certbot

# 3. 写入唯一 HTTP 站点，用 default_server 确保 80 请求都命中这里
sudo tee /etc/nginx/sites-available/koukou-api >/dev/null <<'NGINX'
server {
    listen 80 default_server;
    server_name zzj.abrdns.com _;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
        default_type text/plain;
        allow all;
    }

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
NGINX

sudo ln -sf /etc/nginx/sites-available/koukou-api /etc/nginx/sites-enabled/koukou-api
sudo nginx -t
sudo systemctl restart nginx

# 4. 两个请求都必须是 HTTP 200，并且 body 是 ok
curl -i http://127.0.0.1/.well-known/acme-challenge/ping
curl -i http://zzj.abrdns.com/.well-known/acme-challenge/ping
```

只有当上面两个请求都输出 `HTTP/1.1 200 OK` 和 `ok`，再申请证书：

```bash
sudo certbot certonly --webroot -w /var/www/certbot -d zzj.abrdns.com
```

如果仍失败，使用 `--debug-challenges` 暂停验证，手动检查 Certbot 生成的 challenge 文件是否能被公网读到：

```bash
sudo certbot certonly --webroot -w /var/www/certbot -d zzj.abrdns.com --debug-challenges -v
```

Certbot 暂停并提示继续前，不要按回车。另开一个 SSH 窗口执行：

```bash
ls -la /var/www/certbot/.well-known/acme-challenge
TOKEN=$(ls -t /var/www/certbot/.well-known/acme-challenge | head -n 1)
echo "$TOKEN"
cat "/var/www/certbot/.well-known/acme-challenge/$TOKEN"
curl -i "http://127.0.0.1/.well-known/acme-challenge/$TOKEN"
curl -i "http://zzj.abrdns.com/.well-known/acme-challenge/$TOKEN"
```

两个 `curl` 都必须返回 `200 OK`，并且 body 必须和 `cat` 输出一致。确认后，回到 Certbot 窗口按回车继续。

如果 `127.0.0.1` 是 200，但 `zzj.abrdns.com` 不是 200，说明公网请求没有命中这份 Nginx 配置。检查：

```bash
getent hosts zzj.abrdns.com
sudo nginx -T | grep -nE 'server_name|well-known|deny all|listen 80'
```

如果 `zzj.abrdns.com` 返回 200，但 body 是空的，删除旧 challenge 文件后重试：

```bash
sudo rm -f /var/www/certbot/.well-known/acme-challenge/*
echo ok | sudo tee /var/www/certbot/.well-known/acme-challenge/ping >/dev/null
curl -i http://zzj.abrdns.com/.well-known/acme-challenge/ping
sudo certbot certonly --webroot -w /var/www/certbot -d zzj.abrdns.com --debug-challenges -v
```

证书申请成功后，写入 HTTPS/WSS 配置：

```bash
sudo tee /etc/nginx/sites-available/koukou-api >/dev/null <<'NGINX'
server {
    listen 80;
    server_name zzj.abrdns.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        allow all;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name zzj.abrdns.com;

    ssl_certificate /etc/letsencrypt/live/zzj.abrdns.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zzj.abrdns.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ws {
        proxy_pass http://127.0.0.1:8080/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
NGINX

sudo nginx -t
sudo systemctl reload nginx
curl https://zzj.abrdns.com/health
```

检查自动续期：

```bash
sudo certbot renew --dry-run
```

## 12. Android 地址

Release 使用：

```text
API_BASE_URL=https://zzj.abrdns.com
WS_URL=wss://zzj.abrdns.com/ws
```

Debug 模拟器使用：

```text
API_BASE_URL=http://10.0.2.2:8080
WS_URL=ws://10.0.2.2:8080/ws
```

## 13. 常用命令

```bash
pm2 status
pm2 logs koukou-server --lines 100
pm2 restart koukou-server

sudo nginx -t
sudo systemctl restart nginx

curl http://127.0.0.1:8080/health
curl http://127.0.0.1/health
curl https://zzj.abrdns.com/health
```

注意：URL 不能直接在 bash 里执行。

错误写法：

```bash
http://120.26.247.39/health
ws://120.26.247.39/ws
```

正确写法：

```bash
curl http://120.26.247.39/health
curl http://zzj.abrdns.com/health
```

`ws://` / `wss://` 地址给 App 或 WebSocket 测试工具使用。

## 14. 证书失败排查

如果 Certbot 一直返回：

```text
Invalid response ... 403
```

先确认 80 端口到底是谁在监听。不要用 `grep ':80'`，它会误匹配 `8080`。用精确命令：

```bash
sudo ss -ltnp 'sport = :80'
sudo ss -ltnp 'sport = :443'
```

如果服务器装了宝塔面板，会看到类似进程：

```text
/www/server/panel/BT-Panel
/www/server/panel/BT-Task
```

宝塔面板或宝塔防火墙可能会影响站点/端口访问。先检查：

```bash
ps aux | grep -Ei 'bt|panel|nginx|openresty|apache|httpd|waf' | grep -v grep
sudo ufw status verbose
```

使用 Certbot standalone 前，确保没有任何服务监听 80：

```bash
sudo systemctl stop nginx || true
sudo systemctl stop openresty || true
sudo systemctl stop apache2 || true
sudo systemctl stop httpd || true
sudo ss -ltnp 'sport = :80'
```

如果 `sport = :80` 没有输出，再执行：

```bash
sudo certbot certonly --standalone --preferred-challenges http --http-01-address 0.0.0.0 --http-01-port 80 -d zzj.abrdns.com -v
```

如果仍然 403，检查阿里云是否启用了云防火墙、WAF、宝塔防火墙或安全防护规则；这些可能在请求到达 Certbot 前返回 403。

宝塔面板安全建议：

```text
宝塔面板端口不建议公网开放给所有人。
如果不用宝塔部署本项目，可以在阿里云安全组关闭 8888/888/20/21/39000-40000。
本项目只需要 22/80/443。
```

### HTTP-01 一直 403 时：改用 DNS-01

如果 `webroot` 和 `standalone` 都失败，且错误一直是：

```text
Invalid response ... 403
```

直接改用 DNS 验证。DNS-01 不依赖服务器 80/443，不受 Nginx、宝塔、防火墙影响。

执行：

```bash
sudo certbot certonly --manual --preferred-challenges dns -d zzj.abrdns.com
```

Certbot 会提示你添加一条 TXT 记录，格式类似：

```text
_acme-challenge.zzj.abrdns.com
值：一长串随机字符串
```

到 CloudNS 控制台添加记录：

```text
Type：TXT
Host：_acme-challenge
Points to / Text：Certbot 提供的那一长串值
TTL：60 或 1h
```

添加后不要立刻按回车，先在服务器验证 DNS 是否生效：

```bash
sudo apt install -y dnsutils
nslookup -type=TXT _acme-challenge.zzj.abrdns.com 8.8.8.8
```

看到 TXT 值已经返回后，再回到 Certbot 窗口按回车继续。

证书成功后仍然使用同一份 Nginx HTTPS 配置：

```text
ssl_certificate /etc/letsencrypt/live/zzj.abrdns.com/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/zzj.abrdns.com/privkey.pem;
```

注意：手动 DNS-01 证书不能完全自动续期。到期前需要重新运行同样命令并更新 TXT 记录，或者以后再接 CloudNS API 做自动 DNS 验证。

当前证书已通过 DNS-01 成功签发：

```text
Certificate: /etc/letsencrypt/live/zzj.abrdns.com/fullchain.pem
Key:         /etc/letsencrypt/live/zzj.abrdns.com/privkey.pem
Expires:     2026-08-10
```

证书路径不是命令，不要直接执行：

```bash
/etc/letsencrypt/live/zzj.abrdns.com/fullchain.pem
/etc/letsencrypt/live/zzj.abrdns.com/privkey.pem
```

它们只用于 Nginx 配置：

```nginx
ssl_certificate /etc/letsencrypt/live/zzj.abrdns.com/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/zzj.abrdns.com/privkey.pem;
```

## 15. 最终检查

```text
[ ] 阿里云安全组已开放 22/80/443
[ ] 阿里云安全组未开放 8080/3306/6379
[ ] Redis ping 返回 PONG
[ ] pm2 status 显示 koukou-server online
[ ] curl http://127.0.0.1:8080/health 正常
[ ] curl http://zzj.abrdns.com/health 正常
[x] curl https://zzj.abrdns.com/health 正常
[ ] Android release 地址是 https/wss 的 zzj.abrdns.com
```

当前 HTTPS 测试结果：

```bash
curl http://zzj.abrdns.com/health
# 返回 301 Moved Permanently，正常，表示 HTTP 会跳转 HTTPS

curl https://zzj.abrdns.com/health
# 返回 {"ok":true,"service":"koukou-server","time":...}
```

## 16. 下一步：直到 App 真机通讯成功

当前服务器已经完成：

```text
[x] Node 服务本机 8080 正常
[x] Nginx HTTPS 正常
[x] https://zzj.abrdns.com/health 正常
[x] WebSocket 公网地址确定为 wss://zzj.abrdns.com/ws
```

### 16.1 确认服务端仍在线

服务器执行：

```bash
pm2 status
curl http://127.0.0.1:8080/health
curl https://zzj.abrdns.com/health
```

如果 `pm2 status` 不是 `online`：

```bash
cd /opt/koukou/koukou-server
pm2 start ecosystem.config.js --env production --update-env
pm2 save
```

### 16.2 确认服务端允许当前 MVP token

当前客户端 MVP 阶段用 `userId` 作为 WebSocket token，所以服务器 `.env` 必须是：

```text
DEV_AUTH_ALLOW_USER_ID=true
```

服务器检查：

```bash
cd /opt/koukou/koukou-server
grep DEV_AUTH_ALLOW_USER_ID .env
```

如果不是 true：

```bash
sed -i 's/^DEV_AUTH_ALLOW_USER_ID=.*/DEV_AUTH_ALLOW_USER_ID=true/' .env
pm2 restart koukou-server --update-env
pm2 save
```

### 16.3 确认 Android 地址

本地项目 `app/build.gradle.kts` 的 release 应为：

```kotlin
release {
    buildConfigField("String", "API_BASE_URL", "\"https://zzj.abrdns.com\"")
    buildConfigField("String", "WS_URL", "\"wss://zzj.abrdns.com/ws\"")
}
```

注意：debug 默认如果还是 `10.0.2.2`，真机 debug 包不会连云服务器。真机测试云服务器时，可以临时把 debug 也改成：

```kotlin
debug {
    buildConfigField("String", "API_BASE_URL", "\"https://zzj.abrdns.com\"")
    buildConfigField("String", "WS_URL", "\"wss://zzj.abrdns.com/ws\"")
}
```

### 16.4 提交并推送最新配置

本地执行：

```bash
git status
git add app/build.gradle.kts 云服务器服务部署指南.md
git commit -m "Update cloud deployment guide"
git push origin main
```

如果提示没有东西可提交，说明已经提交过，可以跳过。

如果 push 需要代理：

```bash
git config --global http.proxy http://127.0.0.1:10808
git config --global https.proxy http://127.0.0.1:10808
git push origin main
```

### 16.5 服务器拉取最新代码

服务器执行：

```bash
cd /opt/koukou
git pull origin main
cd /opt/koukou/koukou-server
npm install
pm2 restart koukou-server --update-env
pm2 save
curl https://zzj.abrdns.com/health
```

### 16.6 本地构建 App

本地项目根目录执行：

```bash
.\gradlew.bat clean assembleDebug
```

Debug 包路径：

```text
app/build/outputs/apk/debug/app-debug.apk
```

如果要构建 release：

```bash
.\gradlew.bat assembleRelease
```

当前项目没有签名配置时，先用 Android Studio 运行到真机，或安装 debug 包测试。

### 16.7 真机双账号测试

准备两台手机，或一台真机加一个模拟器，登录两个不同账号：

```text
账号 A：1000000001
账号 B：1000000002
```

测试流程：

```text
1. A 登录，进入联系人/会话。
2. B 登录，进入联系人/会话。
3. A 打开 B 的聊天页，发送文本。
4. A 端消息先显示发送中，再变为已发送。
5. B 端实时收到消息。
6. 关闭 B 的 App 或断网。
7. A 再发一条消息。
8. B 重新打开 App，应同步离线消息。
```

服务器观察日志：

```bash
pm2 logs koukou-server --lines 100
```

### 16.8 如果 App 发送失败

先查服务器：

```bash
curl https://zzj.abrdns.com/health
pm2 status
pm2 logs koukou-server --lines 100
```

再查 Android 构建地址：

```text
release 必须是 wss://zzj.abrdns.com/ws
debug 如果仍是 ws://10.0.2.2:8080/ws，真机 debug 会连不到云服务器
```

### 16.9 如果 WebSocket 不通

服务器确认 `/ws` 配置存在：

```bash
sudo nginx -T | grep -nA12 'location /ws'
```

确认 443 监听：

```bash
sudo ss -ltnp 'sport = :443'
```

确认阿里云安全组开放 443。

### 16.10 证书续期提醒

当前证书到期：

```text
2026-08-10
```

因为使用 DNS-01 手动验证，到期前重新执行：

```bash
sudo certbot certonly --manual --preferred-challenges dns -d zzj.abrdns.com
```

按提示到 CloudNS 更新 TXT：

```text
Type：TXT
Host：_acme-challenge
Text：Certbot 给的新值
```

TXT 生效后按回车继续。成功后：

```bash
sudo nginx -t
sudo systemctl reload nginx
curl https://zzj.abrdns.com/health
```
