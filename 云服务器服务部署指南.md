# Koukou 云服务器服务部署指南

本文档记录当前已经跑通的 `PM2 + 宿主机 MySQL + Nginx + HTTPS/WSS` 部署方式，适用于当前服务器：

```text
云服务器：阿里云 ECS
系统：Ubuntu 22.04 LTS
公网 IP：120.26.247.39
私网 IP：172.30.151.177
域名：zzj.abrdns.com
服务目录：/opt/koukou/koukou-server
API 地址：https://zzj.abrdns.com
WebSocket 地址：wss://zzj.abrdns.com/ws
```

## 1. 安全组建议

阿里云安全组只开放：

```text
22/tcp
80/tcp
443/tcp
```

不要开放：

```text
8080
3306
6379
```

原因：

- `8080` 仅供 Nginx 本机反代
- `3306` 仅供本机 MySQL 使用
- `6379` 当前不对公网开放

## 2. 登录服务器

```bash
ssh root@120.26.247.39
```

## 3. 安装基础环境

```bash
sudo apt update
sudo apt install -y curl git nginx mysql-server ca-certificates build-essential certbot python3-certbot-nginx

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm i -g pm2

node -v
npm -v
pm2 -v
```

## 4. 初始化 MySQL

当前示例数据库账号：

```text
数据库名：koukou_im
数据库用户：koukou
数据库密码：123456
```

执行：

```bash
sudo mysql <<'SQL'
CREATE DATABASE IF NOT EXISTS koukou_im
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

CREATE USER IF NOT EXISTS 'koukou'@'localhost' IDENTIFIED WITH caching_sha2_password BY '123456';
ALTER USER 'koukou'@'localhost' IDENTIFIED WITH caching_sha2_password BY '123456';
GRANT ALL PRIVILEGES ON koukou_im.* TO 'koukou'@'localhost';
FLUSH PRIVILEGES;
SQL
```

验证：

```bash
mysql -u koukou -p123456 -D koukou_im -e "SELECT 1;"
```

## 5. 拉取代码

```bash
cd /opt
rm -rf koukou
git clone https://github.com/tankingTC/koukou.git koukou
cd /opt/koukou/koukou-server
npm install
```

导入表结构：

```bash
mysql -u koukou -p123456 koukou_im < /opt/koukou/koukou-server/sql/init.sql
```

## 6. 写入服务端环境变量

```bash
cd /opt/koukou/koukou-server

cat > .env <<'EOF'
PORT=8080
HOST=0.0.0.0
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=koukou
MYSQL_PASSWORD=123456
MYSQL_DB=koukou_im
JWT_SECRET=replace_me_with_a_long_random_secret
DEV_AUTH_ALLOW_USER_ID=false
EOF
```

生成随机 JWT 密钥：

```bash
sed -i "s/replace_me_with_a_long_random_secret/$(openssl rand -hex 32)/" .env
```

确认：

```bash
grep -E 'PORT|HOST|MYSQL_HOST|MYSQL_PORT|MYSQL_USER|MYSQL_DB|DEV_AUTH_ALLOW_USER_ID' .env
```

说明：

- 当前客户端已经优先使用服务端返回的 JWT token
- 因此生产环境建议：`DEV_AUTH_ALLOW_USER_ID=false`

## 7. 启动服务端

```bash
mkdir -p /var/log/koukou-server

cd /opt/koukou/koukou-server
pm2 delete koukou-server || true
pm2 start ecosystem.config.js --env production --update-env
pm2 save
pm2 status
```

健康检查：

```bash
curl http://127.0.0.1:8080/health
```

正常返回：

```json
{"ok":true,"service":"koukou-server","time":...}
```

## 8. 配置 Nginx

```bash
sudo rm -f /etc/nginx/sites-enabled/default

sudo tee /etc/nginx/sites-available/koukou-api >/dev/null <<'NGINX'
server {
    listen 80;
    server_name zzj.abrdns.com;

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name zzj.abrdns.com;

    ssl_certificate /etc/letsencrypt/live/zzj.abrdns.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zzj.abrdns.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ws {
        proxy_pass http://127.0.0.1:8080/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
NGINX

sudo ln -sf /etc/nginx/sites-available/koukou-api /etc/nginx/sites-enabled/koukou-api
sudo nginx -t
sudo systemctl restart nginx
```

## 9. HTTPS/WSS 检查

```bash
curl https://zzj.abrdns.com/health
```

正常返回：

```json
{"ok":true,"service":"koukou-server","time":...}
```

## 10. 当前真实联调状态

当前线上已经确认：

- `http://127.0.0.1:8080/health` 正常
- `https://zzj.abrdns.com/health` 在服务器内正常
- `https://zzj.abrdns.com/health` 在部分外部网络下存在被 reset 的情况

因此当前客户端已经加了备用线路：

```text
主线路：
  https://zzj.abrdns.com
  wss://zzj.abrdns.com/ws

备用线路：
  https://120.26.247.39
  wss://120.26.247.39/ws
```

说明：

- 这是客户端的可用性兜底
- 服务器侧仍建议后续继续排查主域名在外部网络被 reset 的问题

## 11. 常用更新部署命令

以后每次代码更新后，服务器执行：

```bash
cd /opt/koukou
git pull origin main

cd /opt/koukou/koukou-server
npm install
mysql -u koukou -p123456 koukou_im < /opt/koukou/koukou-server/sql/init.sql

pm2 restart koukou-server --update-env
pm2 save
pm2 status

sudo nginx -t
sudo systemctl reload nginx

curl http://127.0.0.1:8080/health
curl https://zzj.abrdns.com/health
```

## 12. 日志排查

查看服务端状态：

```bash
pm2 status
pm2 logs koukou-server --lines 100
```

清理旧错误日志：

```bash
pm2 flush koukou-server
```

## 13. Android 联调重点

客户端当前应使用：

```text
API_BASE_URL=https://zzj.abrdns.com
API_BASE_URL_BACKUP=https://120.26.247.39
WS_URL=wss://zzj.abrdns.com/ws
WS_URL_BACKUP=wss://120.26.247.39/ws
```

当出现以下问题时，优先排查：

### 13.1 注册/登录提示网络异常

优先检查：

- `curl https://zzj.abrdns.com/health`
- `curl http://127.0.0.1:8080/health`
- `pm2 status`
- `.env` 中 `MYSQL_PASSWORD` 是否为空

### 13.2 登录成功后主页面闪退

优先检查：

- `/ws` 是否可连
- 服务端首帧消息是否符合客户端字段结构
- `.env` 中 `JWT_SECRET` 是否存在
- 客户端是否安装了最新 APK

### 13.3 好友或聊天异常

优先检查：

- `/api/friends`
- `/api/friends/requests`
- WebSocket 日志中的 `chat_message` / `message_ack`

## 14. 最终检查清单

```text
[ ] 22/80/443 已放行
[ ] 8080/3306 未对公网开放
[ ] MySQL 账号 koukou / 123456 可登录
[ ] pm2 status 显示 koukou-server online
[ ] curl http://127.0.0.1:8080/health 正常
[ ] curl https://zzj.abrdns.com/health 正常
[ ] Android 客户端已使用最新 API / WS 双线路配置
```

---

文档更新时间：2026-05-17  
适用范围：当前 `koukou-server` 宿主机 PM2 + Nginx + MySQL 部署方案
