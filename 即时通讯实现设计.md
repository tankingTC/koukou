# Koukou 即时通讯功能实现设计

## 文档版本

- v1.0 初版：通用 IM 设计（客户端协议、状态机、Room、WebSocket、通用云端架构）。
- v1.1 落地版：新增 §21「本项目实际部署方案 (Aliyun ECS + Node.js + MySQL + Redis)」，对应当前已就绪的阿里云 ECS 环境。§20 保留为通用推荐架构，§21 是 Koukou 项目的具体选型与待办清单。

## 1. 目标

本文档用于设计 Koukou 的即时通讯能力如何从当前 Demo 逐步扩展为更完整、可靠、可维护的聊天系统。

核心目标：

- 支持用户之间实时收发消息。
- 支持消息本地持久化。
- 支持会话列表实时更新。
- 支持消息状态：发送中、已发送、已送达、已读、失败。
- 支持 WebSocket 连接、断线重连、心跳保活。
- 支持离线消息同步。
- 支持通知、未读数、会话置顶等体验能力。
- 保持当前 Android 原生 Java + XML、MVVM、Repository、Room、DataStore、WebSocket 架构风格。

## 2. 当前项目基础

当前 Koukou 已经具备以下即时通讯基础：

- `MessageEntity`：本地消息实体。
- `ConversationEntity`：会话实体。
- `MessageDao`：消息查询和插入。
- `ConversationDao`：会话查询、更新、未读数。
- `MessageRepository`：消息收发和会话摘要更新。
- `WebSocketManager`：WebSocket 连接、发送和监听分发。
- `ChatActivity`：聊天页面。
- `MessageAdapter`：聊天消息列表展示。
- `ConversationsFragment`：会话列表。

当前实现适合作为基础版，但还需要增强协议、状态、ACK、重连和离线同步。

## 3. 整体架构

推荐分层如下：

```text
UI 层
├─ ChatActivity
├─ ConversationsFragment
└─ ContactsFragment

ViewModel 层
├─ ChatViewModel
├─ ConversationsViewModel
└─ ContactsViewModel

Repository 层
├─ MessageRepository
├─ ConversationRepository
├─ ContactRepository
└─ UserRepository

本地数据层
├─ Room Database
├─ MessageDao
├─ ConversationDao
├─ FriendDao
└─ UserDao

网络层
├─ WebSocketManager
├─ WebSocketMessage
└─ AppWebSocketListener
```

消息流转应遵循：

```text
用户发送消息
  ↓
ChatActivity
  ↓
ChatViewModel
  ↓
MessageRepository
  ↓                 ↘
Room 本地落库       WebSocket 发送
  ↓                 ↓
LiveData 刷新 UI    服务端 ACK
                    ↓
              更新消息状态
                    ↓
              LiveData 刷新 UI
```

接收消息流转：

```text
WebSocket 收到消息
  ↓
WebSocketManager 分发
  ↓
MessageRepository 处理
  ↓
写入 MessageEntity
  ↓
更新 ConversationEntity
  ↓
ChatActivity / ConversationsFragment 自动刷新
  ↓
后台时触发通知
```

## 4. 消息数据设计

### 4.1 MessageEntity 建议字段

当前已有字段可以继续使用，并建议扩展为：

```text
messageId: String
clientMessageId: String
conversationId: String
senderId: String
receiverId: String
content: String
msgType: String
localPath: String
timestamp: long
chatType: String
status: String
isRead: boolean
retryCount: int
serverTimestamp: long
```

字段说明：

- `messageId`：服务端消息 ID，服务端确认后写入。
- `clientMessageId`：客户端本地生成 ID，用于 ACK 匹配。
- `conversationId`：会话 ID。
- `senderId`：发送方。
- `receiverId`：接收方，群聊时可以是 groupId。
- `content`：消息内容。
- `msgType`：消息类型，如 text、image、video、emoji、file。
- `localPath`：本地媒体文件路径。
- `timestamp`：客户端发送时间。
- `chatType`：single 或 group。
- `status`：sending、sent、delivered、read、failed。
- `isRead`：本地是否已读。
- `retryCount`：重发次数。
- `serverTimestamp`：服务端时间。

### 4.2 ConversationEntity 建议字段

```text
conversationId: String
ownerId: String
targetId: String
targetName: String
targetAvatarUrl: String
lastMessage: String
lastMessageTime: long
unreadCount: int
isPinned: boolean
isMuted: boolean
chatType: String
draft: String
```

字段说明：

- `ownerId`：当前用户。
- `targetId`：对方用户 ID 或群 ID。
- `lastMessage`：会话摘要。
- `unreadCount`：未读数量。
- `isPinned`：置顶。
- `isMuted`：免打扰。
- `chatType`：single/group。
- `draft`：草稿。

## 5. WebSocket 协议设计

### 5.1 通用消息结构

建议统一 WebSocket 消息结构：

```json
{
  "type": "chat_message",
  "clientMessageId": "local_1710000000000_001",
  "messageId": "server_msg_001",
  "fromUserId": "1000000001",
  "toUserId": "1000000002",
  "conversationId": "single_1000000001_1000000002",
  "chatType": "single",
  "msgType": "text",
  "content": "你好",
  "timestamp": 1710000000000,
  "extra": {}
}
```

### 5.2 type 类型

建议支持：

```text
chat_message        普通聊天消息
message_ack         消息发送确认
message_delivered   消息送达回执
message_read        消息已读回执
typing              正在输入
heartbeat_ping      心跳请求
heartbeat_pong      心跳响应
friend_request      好友申请
profile_update      资料更新
error               错误消息
```

### 5.3 ACK 消息

客户端发送消息后，服务端返回：

```json
{
  "type": "message_ack",
  "clientMessageId": "local_1710000000000_001",
  "messageId": "server_msg_001",
  "status": "sent",
  "serverTimestamp": 1710000000200
}
```

客户端收到 ACK 后：

- 根据 `clientMessageId` 找到本地消息。
- 更新 `messageId`。
- 更新 `status = sent`。
- 更新 `serverTimestamp`。
- 刷新聊天页面。

### 5.4 已读回执

进入聊天页面后可以发送：

```json
{
  "type": "message_read",
  "conversationId": "single_1000000001_1000000002",
  "readerId": "1000000002",
  "lastReadMessageId": "server_msg_100"
}
```

收到后：

- 更新自己发送消息的 `status = read`。
- 清除当前会话未读数。

## 6. 发送消息流程

### 6.1 文本消息发送

流程：

1. 用户在 `ChatActivity` 输入文本。
2. 点击发送按钮。
3. `ChatViewModel.sendTextMessage()` 调用 `MessageRepository.sendMessage()`。
4. 本地生成 `clientMessageId`。
5. 构造 `MessageEntity`，状态为 `sending`。
6. 插入 Room。
7. 更新会话摘要。
8. 通过 WebSocket 发送。
9. 等待服务端 ACK。
10. ACK 成功后更新状态为 `sent`。
11. 超时或失败则更新为 `failed`。

### 6.2 推荐伪代码

```java
public void sendTextMessage(String receiverId, String content) {
    String clientMessageId = generateClientMessageId();
    MessageEntity message = new MessageEntity();
    message.clientMessageId = clientMessageId;
    message.senderId = currentUserId;
    message.receiverId = receiverId;
    message.content = content;
    message.msgType = "text";
    message.status = "sending";
    message.timestamp = System.currentTimeMillis();

    messageDao.insert(message);
    conversationRepository.updateLastMessage(message);

    boolean sent = webSocketManager.sendMessage(toWebSocketJson(message));
    if (!sent) {
        messageDao.updateStatus(clientMessageId, "failed");
    }
}
```

## 7. 接收消息流程

流程：

1. `WebSocketManager` 收到原始 JSON。
2. 解析为 `WebSocketMessage`。
3. 判断 `type`。
4. 如果是 `chat_message`，交给 `MessageRepository.handleIncomingMessage()`。
5. 转换为 `MessageEntity`。
6. 插入 Room。
7. 更新或创建 `ConversationEntity`。
8. 如果当前不在对应聊天页，未读数 +1。
9. 如果 App 在后台，触发通知。
10. 发送 delivered 回执。

### 7.1 接收消息去重

需要避免重复插入。

建议：

- 优先根据 `messageId` 去重。
- 没有 `messageId` 时根据 `clientMessageId + senderId` 去重。

## 8. 消息状态机

推荐状态流转：

```text
sending
  ↓ WebSocket 发送成功并收到 ACK
sent
  ↓ 对方客户端收到
 delivered
  ↓ 对方打开会话
read
```

失败流转：

```text
sending
  ↓ 发送失败/超时
failed
  ↓ 用户点击重试
sending
```

状态 UI：

- `sending`：显示“发送中...”或小菊花。
- `sent`：显示“已发送”。
- `delivered`：显示“已送达”。
- `read`：显示“已读”。
- `failed`：显示红色失败图标，可点击重试。

## 9. WebSocket 连接管理

### 9.1 连接时机

建议：

- 登录成功后连接。
- MainActivity 创建时确认连接。
- App 切到后台可保持连接一段时间。
- 退出登录时断开连接。

### 9.2 心跳机制

客户端每 20-30 秒发送：

```json
{
  "type": "heartbeat_ping",
  "timestamp": 1710000000000
}
```

服务端返回：

```json
{
  "type": "heartbeat_pong",
  "timestamp": 1710000000100
}
```

如果连续多次未收到 pong：

- 标记连接断开。
- 关闭旧连接。
- 触发重连。

### 9.3 重连策略

建议指数退避：

```text
第 1 次：1 秒
第 2 次：2 秒
第 3 次：4 秒
第 4 次：8 秒
之后：最多 30 秒一次
```

重连成功后：

- 同步离线消息。
- 重发本地 `sending` 状态超过一定时间的消息。
- 刷新会话列表。

## 10. 离线消息同步

WebSocket 断开期间可能错过消息。

建议服务端提供接口或 WebSocket 指令：

```json
{
  "type": "sync_request",
  "userId": "1000000001",
  "lastMessageTime": 1710000000000
}
```

服务端返回：

```json
{
  "type": "sync_response",
  "messages": []
}
```

客户端处理：

- 批量去重插入 Room。
- 更新所有会话摘要。
- 重新计算未读数。

## 11. 本地 Room 设计建议

### 11.1 MessageDao 增强

建议增加：

```java
@Query("SELECT * FROM messages WHERE conversationId = :conversationId ORDER BY timestamp ASC")
LiveData<List<MessageEntity>> observeMessages(String conversationId);

@Query("UPDATE messages SET status = :status WHERE clientMessageId = :clientMessageId")
void updateStatusByClientId(String clientMessageId, String status);

@Query("SELECT * FROM messages WHERE status = 'sending' OR status = 'failed'")
List<MessageEntity> getPendingMessages();
```

### 11.2 ConversationDao 增强

建议增加：

```java
@Query("SELECT * FROM conversations WHERE ownerId = :ownerId ORDER BY isPinned DESC, lastMessageTime DESC")
LiveData<List<ConversationEntity>> observeConversations(String ownerId);

@Query("UPDATE conversations SET unreadCount = 0 WHERE conversationId = :conversationId")
void clearUnread(String conversationId);

@Query("UPDATE conversations SET isPinned = :pinned WHERE conversationId = :conversationId")
void setPinned(String conversationId, boolean pinned);
```

## 12. 会话 ID 设计

单聊建议：

```text
single_minUserId_maxUserId
```

示例：

```text
single_1000000001_1000000002
```

优点：

- 双方生成一致。
- 避免重复会话。

群聊建议：

```text
group_groupId
```

## 13. UI 设计

### 13.1 ChatActivity

需要展示：

- 消息列表。
- 输入框。
- 发送按钮。
- 媒体按钮。
- 表情按钮。
- 连接状态提示。
- 对方正在输入状态。

### 13.2 MessageAdapter

需要支持：

- 文本消息。
- 图片消息。
- 视频消息。
- 表情消息。
- 文件消息。
- 发送状态。
- 失败重试。
- 长按菜单。

### 13.3 ConversationsFragment

需要支持：

- 会话列表实时更新。
- 未读数角标。
- 置顶排序。
- 免打扰图标。
- 删除会话。
- 搜索会话。

## 14. 通知设计

后台收到消息时：

1. 判断通知总开关。
2. 判断该会话是否免打扰。
3. 判断消息预览开关。
4. 创建通知。
5. 点击通知进入 `ChatActivity`。

通知内容：

- 预览开启：显示发送者和消息内容。
- 预览关闭：显示“你收到一条新消息”。

Android 13+：

- 需要申请 `POST_NOTIFICATIONS` 权限。

## 15. 错误处理

需要处理：

- WebSocket 未连接。
- 消息 JSON 解析失败。
- 本地数据库写入失败。
- 发送超时。
- ACK 丢失。
- 重复消息。
- 用户已被拉黑。
- 好友关系不存在。

建议统一错误封装：

```text
errorCode
errorMessage
recoverable
```

## 16. 安全与权限

建议：

- 消息发送前校验当前登录用户。
- 服务端验证 token 或 session。
- 不信任客户端传入的 senderId。
- 图片/文件上传需要限制大小和类型。
- 本地敏感配置不要明文存储。

## 17. 服务端配合设计

客户端完整即时通讯需要服务端支持：

- WebSocket 连接鉴权。
- 用户在线状态维护。
- 消息转发。
- 消息持久化。
- ACK 回执。
- 离线消息缓存。
- 好友关系校验。
- 群聊路由。

基础服务端模块：

```text
UserService
FriendService
MessageService
ConversationService
WebSocketSessionManager
OfflineMessageStore
```

## 18. 推荐开发顺序

### 第一阶段：可靠单聊

1. 规范 WebSocket 协议。
2. 增加 `clientMessageId`。
3. 实现 ACK。
4. 实现发送失败状态。
5. 实现失败重试。
6. 收到消息后本地去重。

### 第二阶段：连接稳定性

1. 心跳 ping/pong。
2. 断线重连。
3. 连接状态展示。
4. 重连后同步离线消息。

### 第三阶段：体验增强

1. 本地通知。
2. 会话置顶。
3. 会话免打扰。
4. 消息已读回执。
5. 正在输入提示。

### 第四阶段：内容增强

1. 图片预览。
2. 视频预览。
3. 表情面板。
4. 文件消息。
5. 长按菜单。

### 第五阶段：群聊

1. 群实体。
2. 群成员。
3. 群消息路由。
4. 群资料页。
5. 群管理功能。

## 19. 最小可行版本 MVP

如果要最快实现一个稳定可用的即时通讯版本，建议 MVP 包含：

- 文本单聊。
- 本地消息保存。
- 会话列表更新。
- 未读数。
- WebSocket 实时收发。
- ACK 更新为已发送。
- 发送失败重试。
- 断线重连。

暂时不做：

- 群聊。
- 已读回执。
- 正在输入。
- 文件消息。
- 复杂通知分组。

## 20. 云服务器部署在线通讯实现方案

### 20.1 部署目标

云服务器部署的目标是让不同设备、不同网络环境下的 Koukou 客户端都能通过公网服务器完成实时通讯。

部署完成后应支持：

- 公网 WebSocket 长连接。
- 用户登录后绑定在线连接。
- 单聊消息实时转发。
- 用户离线时消息入库。
- 用户重新上线后同步离线消息。
- 消息 ACK 确认。
- 断线重连。
- HTTPS/WSS 安全访问。

### 20.2 推荐云端架构

推荐基础架构：

```text
Android 客户端
  ↓ HTTPS / WSS
Nginx 反向代理
  ↓
后端服务 Spring Boot / Node.js / Go
  ↓
数据库 MySQL / PostgreSQL
  ↓
Redis 可选，用于在线状态和消息队列
```

最小可行版本可以先使用：

- 1 台云服务器。
- 1 个后端服务。
- 1 个 MySQL 数据库。
- 1 个 WebSocket 服务端口。
- Nginx 做 HTTPS 和 WSS 代理。

后续用户量变大后再拆分为：

- API 服务。
- WebSocket 网关。
- 消息服务。
- 用户服务。
- Redis。
- 对象存储。

### 20.3 云服务器选择

推荐配置：

```text
CPU：2 核
内存：2GB 或 4GB
系统：Ubuntu 22.04 LTS
带宽：3Mbps 起步
磁盘：40GB 起步
```

适合选择：

- 阿里云 ECS。
- 腾讯云 CVM。
- 华为云 ECS。
- 火山云服务器。
- 轻量应用服务器。

测试阶段可以使用轻量服务器，正式部署建议使用标准云服务器。

### 20.4 域名和端口规划

推荐域名：

```text
API 地址：https://api.yourdomain.com
WebSocket 地址：wss://api.yourdomain.com/ws
```

服务端内部端口示例：

```text
后端 HTTP：127.0.0.1:8080
WebSocket：127.0.0.1:8080/ws
Nginx 对外：443
```

安全组需要开放：

```text
22    SSH
80    HTTP，用于证书申请和跳转
443   HTTPS/WSS
```

不建议直接暴露：

```text
3306  MySQL
6379  Redis
8080  后端服务端口
```

这些端口应只允许本机或内网访问。

### 20.5 服务端核心模块

云端服务至少需要以下模块：

```text
AuthController        登录鉴权
UserController        用户资料
FriendController      好友关系
MessageController     离线消息拉取
WebSocketEndpoint     WebSocket 连接入口
SessionManager        在线连接管理
MessageService        消息处理
OfflineMessageStore   离线消息保存
AckService            ACK 回执处理
```

### 20.6 WebSocket 在线连接管理

服务端需要维护用户 ID 和 WebSocket Session 的映射：

```text
userId -> WebSocketSession
```

用户连接流程：

```text
客户端连接 wss://api.yourdomain.com/ws?token=xxx
  ↓
服务端校验 token
  ↓
解析 userId
  ↓
保存 userId 和 session 的映射
  ↓
标记用户在线
  ↓
推送离线消息
```

断开流程：

```text
WebSocket 断开
  ↓
移除 session
  ↓
标记用户离线
```

如果同一账号多设备登录，可以改为：

```text
userId -> List<WebSocketSession>
```

这样手机、平板、模拟器可以同时在线。

### 20.7 单聊消息转发流程

客户端发送：

```json
{
  "type": "chat_message",
  "clientMessageId": "local_1710000000000_001",
  "fromUserId": "1000000001",
  "toUserId": "1000000002",
  "conversationId": "single_1000000001_1000000002",
  "chatType": "single",
  "msgType": "text",
  "content": "你好",
  "timestamp": 1710000000000
}
```

服务端处理：

1. 校验发送者身份。
2. 校验好友关系。
3. 生成服务端 `messageId`。
4. 写入数据库。
5. 给发送者返回 ACK。
6. 如果接收者在线，实时推送消息。
7. 如果接收者离线，保留为离线消息。

ACK 示例：

```json
{
  "type": "message_ack",
  "clientMessageId": "local_1710000000000_001",
  "messageId": "server_msg_001",
  "status": "sent",
  "serverTimestamp": 1710000000200
}
```

### 20.8 服务端数据库表设计

#### 用户表

```sql
CREATE TABLE users (
    user_id VARCHAR(32) PRIMARY KEY,
    account VARCHAR(32) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    nickname VARCHAR(64),
    avatar_url TEXT,
    signature TEXT,
    created_at BIGINT NOT NULL,
    updated_at BIGINT NOT NULL
);
```

#### 好友表

```sql
CREATE TABLE friends (
    owner_id VARCHAR(32) NOT NULL,
    friend_id VARCHAR(32) NOT NULL,
    created_at BIGINT NOT NULL,
    PRIMARY KEY (owner_id, friend_id)
);
```

#### 消息表

```sql
CREATE TABLE messages (
    message_id VARCHAR(64) PRIMARY KEY,
    client_message_id VARCHAR(64),
    conversation_id VARCHAR(128) NOT NULL,
    sender_id VARCHAR(32) NOT NULL,
    receiver_id VARCHAR(32) NOT NULL,
    chat_type VARCHAR(16) NOT NULL,
    msg_type VARCHAR(16) NOT NULL,
    content TEXT,
    status VARCHAR(16) NOT NULL,
    created_at BIGINT NOT NULL
);
```

#### 离线消息表

```sql
CREATE TABLE offline_messages (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id VARCHAR(32) NOT NULL,
    message_id VARCHAR(64) NOT NULL,
    delivered BOOLEAN DEFAULT FALSE,
    created_at BIGINT NOT NULL
);
```

### 20.9 Nginx WSS 反向代理配置

示例配置：

```nginx
server {
    listen 80;
    server_name api.yourdomain.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name api.yourdomain.com;

    ssl_certificate /etc/letsencrypt/live/api.yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.yourdomain.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ws {
        proxy_pass http://127.0.0.1:8080/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
```

### 20.10 HTTPS 证书

推荐使用 Let's Encrypt：

```bash
sudo apt update
sudo apt install nginx certbot python3-certbot-nginx
sudo certbot --nginx -d api.yourdomain.com
```

证书会自动生成，并可配置自动续期。

检查续期：

```bash
sudo certbot renew --dry-run
```

### 20.11 Android 客户端地址改造

当前开发环境一般使用：

```text
ws://10.0.2.2:8080/ws
```

部署到云服务器后应改为：

```text
wss://api.yourdomain.com/ws
```

建议在 `BuildConfig` 中区分环境：

```gradle
debug {
    buildConfigField "String", "WS_URL", "\"ws://10.0.2.2:8080/ws\""
    buildConfigField "String", "API_BASE_URL", "\"http://10.0.2.2:8080\""
}

release {
    buildConfigField "String", "WS_URL", "\"wss://api.yourdomain.com/ws\""
    buildConfigField "String", "API_BASE_URL", "\"https://api.yourdomain.com\""
}
```

然后在客户端中使用：

```java
private static final String WS_URL = BuildConfig.WS_URL;
```

这样可以避免每次打包手动修改地址。

### 20.12 鉴权设计

WebSocket 不能裸连，必须带登录凭证。

推荐流程：

```text
用户登录
  ↓
服务端返回 token
  ↓
客户端保存 token
  ↓
WebSocket 连接时携带 token
  ↓
服务端校验 token
```

连接方式：

```text
wss://api.yourdomain.com/ws?token=xxx
```

或者使用 Header：

```text
Authorization: Bearer xxx
```

如果使用 OkHttp WebSocket，可以构建 Request 时加入 Header。

### 20.13 保活策略

客户端：

- 每 20-30 秒发送一次 `heartbeat_ping`。
- 超过 2-3 次未收到 `heartbeat_pong` 视为断线。
- 断线后触发重连。

服务端：

- 收到 ping 后返回 pong。
- 定期清理无效 session。
- 记录最后活跃时间。

### 20.14 离线消息上线同步

用户连接成功后：

1. 查询 `offline_messages`。
2. 找到对应 `messages`。
3. 按时间顺序推送给客户端。
4. 客户端 ACK 收到后，服务端标记 delivered。
5. 可定期清理已投递离线消息。

也可以由客户端主动同步：

```json
{
  "type": "sync_request",
  "lastMessageTime": 1710000000000
}
```

服务端返回：

```json
{
  "type": "sync_response",
  "messages": []
}
```

### 20.15 部署步骤

#### 第一步：准备服务器

```bash
sudo apt update
sudo apt upgrade
sudo apt install openjdk-17-jdk nginx mysql-server
```

#### 第二步：上传后端服务

如果是 Spring Boot：

```bash
java -jar koukou-server.jar
```

建议使用 systemd 管理：

```ini
[Unit]
Description=Koukou Server
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/opt/koukou-server
ExecStart=/usr/bin/java -jar /opt/koukou-server/koukou-server.jar
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

启动：

```bash
sudo systemctl daemon-reload
sudo systemctl enable koukou-server
sudo systemctl start koukou-server
```

#### 第三步：配置 Nginx

配置 HTTPS 和 `/ws` 反向代理。

#### 第四步：配置客户端地址

将 release 环境 WebSocket 地址改为：

```text
wss://api.yourdomain.com/ws
```

#### 第五步：真机测试

测试项：

- 手机流量网络能连接。
- Wi-Fi 网络能连接。
- 两个账号互发消息。
- App 退后台后再回来能重连。
- 断网后恢复能重连。
- 离线消息能同步。

### 20.16 上线检查清单

上线前检查：

- 域名 DNS 已解析到服务器 IP。
- 服务器安全组开放 80 和 443。
- Nginx HTTPS 证书有效。
- `/ws` 支持 Upgrade。
- 后端服务 systemd 可自动重启。
- MySQL 不暴露公网。
- WebSocket 必须鉴权。
- 服务端日志能查看连接和错误。
- 客户端 release 包使用 `wss://`。
- 消息有 ACK。
- 断线能自动重连。
- 离线消息不会重复插入。

### 20.17 常见问题

#### 连接不上云服务器

检查：

- 安全组是否开放 443。
- Nginx 是否启动。
- 域名是否解析正确。
- 证书是否有效。
- 客户端是否使用 `wss://`。

#### WebSocket 连接后马上断开

检查：

- Nginx 是否配置 `Upgrade`。
- `proxy_http_version` 是否为 `1.1`。
- 服务端是否校验 token 失败。
- 服务器日志是否有异常。

#### 模拟器能连，本机不能连

检查：

- App 是否仍使用 `10.0.2.2`。
- release 包是否配置云端地址。
- 手机网络是否能访问域名。

#### 消息重复

解决：

- 客户端按 `messageId` 去重。
- 服务端按 `clientMessageId + senderId` 幂等处理。
- ACK 和 sync 消息要区分类型。

### 20.18 最小云端实现版本

如果只做最小可用在线通讯，建议实现：

- 登录接口返回 token。
- WebSocket 连接鉴权。
- 在线用户 session 管理。
- 文本单聊转发。
- 消息入 MySQL。
- 接收方离线时保存离线消息。
- 上线后推送离线消息。
- 发送方收到 ACK。
- 客户端断线重连。

暂时不做：

- 多设备同步。
- 群聊。
- 已读回执。
- 文件上传。
- 消息撤回。
- 复杂推送通知。

## 21. 本项目实际部署方案 (Aliyun ECS + Node.js + MySQL + Redis)

> 本节是对 §20 通用推荐架构的**落地具体化**。§20 是通用方案（兼容 Spring Boot / Go 等多种栈），本节是 Koukou 项目当前实际选用的栈、当前服务器的真实状态、以及上线前必须完成的待办。

### 21.1 已就绪的服务器环境

云厂商：阿里云 ECS
主机名：`iZbp1fl4qtawqmt6l4vldnZ`
系统：Ubuntu 22.04 LTS
登录方式：SSH 公钥（ECS 默认）

已安装：

- Java (JDK)
- Node.js
- MySQL `8.0.45-0ubuntu0.22.04.1`
- Redis（**需校验**，见 §21.5）

实际观察（截图取证，需修复）：

- 直接执行 `mysql` 即可登录 → root 用户当前**无密码**，生产环境必须立即设置（§21.4）。
- 终端执行 `redis` 提示 command not found —— 实际命令是 `redis-cli`，但仍需校验包是否真的装了（§21.5）。
- ESM Apps 提示有 7 个安全更新未应用，建议在配置完成后执行 `sudo apt upgrade`。
- "2 updates could not be installed automatically" —— 一般是涉及 reboot 的内核更新，最后一次性重启即可。

### 21.2 选型决策：为什么用 Node.js

虽然服务器同时有 JDK 和 Node.js，但本项目后端实际选 Node.js：

- **IM 是 Node.js 的舒适区**：`ws` / `socket.io` 与 MySQL/Redis 客户端轻量协作，代码量小。
- **单文件可启动，部署极简**：适合个人项目和 MVP。
- **内存开销远小于 Spring Boot**：2GB 小机器完全跑得起，留出余量给 MySQL/Redis。

JDK 暂作为 Android 构建产物的备用环境保留；如果未来需要切到 Spring Boot（业务变重、需要复杂事务等），可参考 §20.5 / §20.15 的 Spring Boot 实现路径。

### 21.3 后端项目结构（推荐）

```text
koukou-server/
├─ package.json              # 依赖：express, ws, mysql2, ioredis, jsonwebtoken, bcrypt, dotenv
├─ .env                      # 数据库 / Redis / JWT 密钥（不入仓）
├─ .gitignore
├─ ecosystem.config.js       # pm2 进程配置
├─ src/
│  ├─ index.js               # 入口：Express + WebSocket Server 启动
│  ├─ config.js              # 环境变量集中读取
│  ├─ db.js                  # mysql2 连接池
│  ├─ redis.js               # ioredis 客户端
│  ├─ auth/
│  │  ├─ token.js            # JWT 签发 / 校验
│  │  ├─ password.js         # bcrypt 哈希 / 校验
│  │  └─ middleware.js       # Express 鉴权中间件 + WS upgrade 鉴权
│  ├─ controllers/           # REST 接口
│  │  ├─ authController.js   # POST /api/login  /api/register
│  │  ├─ userController.js   # GET  /api/users/:id
│  │  ├─ friendController.js # /api/friends/*
│  │  └─ messageController.js# /api/messages/sync
│  ├─ ws/
│  │  ├─ server.js           # WebSocketServer + on('connection')
│  │  ├─ sessionManager.js   # userId -> Set<WebSocket>（内存）+ Redis 兜底
│  │  ├─ heartbeat.js        # ping / pong
│  │  └─ handlers/
│  │     ├─ chatMessage.js
│  │     ├─ messageRead.js
│  │     └─ syncRequest.js
│  └─ services/
│     ├─ messageService.js   # 消息写表 / 路由 / 离线
│     └─ offlineStore.js     # offline_messages 表 + Redis 队列
└─ sql/
   └─ init.sql               # 建库建表脚本（§21.4.3）
```

依赖示意（`package.json`）：

```json
{
  "name": "koukou-server",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.0",
    "ws": "^8.18.0",
    "mysql2": "^3.11.0",
    "ioredis": "^5.4.0",
    "jsonwebtoken": "^9.0.0",
    "bcrypt": "^5.1.0",
    "dotenv": "^16.4.0"
  }
}
```

### 21.4 MySQL 初始化（必做）

#### 21.4.1 给 root 设密码

```bash
sudo mysql <<'SQL'
ALTER USER 'root'@'localhost' IDENTIFIED WITH caching_sha2_password BY '换成强密码';
FLUSH PRIVILEGES;
SQL
```

之后再执行 `mysql -u root -p` 才能登录，`mysql` 裸命令将不再可用。

#### 21.4.2 建业务库与专用账号

应用代码**禁止**用 root 连库，改用最小权限的 `koukou` 账号：

```sql
CREATE DATABASE koukou_im
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

CREATE USER 'koukou'@'localhost' IDENTIFIED WITH caching_sha2_password BY '应用密码';
GRANT ALL PRIVILEGES ON koukou_im.* TO 'koukou'@'localhost';
FLUSH PRIVILEGES;
```

应用密码写到 `koukou-server/.env`，**不入仓**：

```text
MYSQL_HOST=127.0.0.1
MYSQL_USER=koukou
MYSQL_PASSWORD=应用密码
MYSQL_DB=koukou_im
```

#### 21.4.3 建表（基于 §20.8 + 索引补强）

```sql
USE koukou_im;

CREATE TABLE users (
    user_id        VARCHAR(32)  PRIMARY KEY,
    account        VARCHAR(32)  UNIQUE NOT NULL,
    password_hash  VARCHAR(255) NOT NULL,
    nickname       VARCHAR(64),
    avatar_url     TEXT,
    signature      TEXT,
    created_at     BIGINT       NOT NULL,
    updated_at     BIGINT       NOT NULL
) ENGINE=InnoDB;

CREATE TABLE friends (
    owner_id   VARCHAR(32) NOT NULL,
    friend_id  VARCHAR(32) NOT NULL,
    created_at BIGINT      NOT NULL,
    PRIMARY KEY (owner_id, friend_id),
    INDEX idx_friends_owner (owner_id)
) ENGINE=InnoDB;

CREATE TABLE messages (
    message_id        VARCHAR(64)  PRIMARY KEY,
    client_message_id VARCHAR(64),
    conversation_id   VARCHAR(128) NOT NULL,
    sender_id         VARCHAR(32)  NOT NULL,
    receiver_id       VARCHAR(32)  NOT NULL,
    chat_type         VARCHAR(16)  NOT NULL,
    msg_type          VARCHAR(16)  NOT NULL,
    content           TEXT,
    status            VARCHAR(16)  NOT NULL,
    created_at        BIGINT       NOT NULL,
    INDEX idx_msg_conv_time (conversation_id, created_at),
    INDEX idx_msg_client (client_message_id, sender_id)
) ENGINE=InnoDB;

CREATE TABLE offline_messages (
    id          BIGINT      PRIMARY KEY AUTO_INCREMENT,
    user_id     VARCHAR(32) NOT NULL,
    message_id  VARCHAR(64) NOT NULL,
    delivered   BOOLEAN     DEFAULT FALSE,
    created_at  BIGINT      NOT NULL,
    INDEX idx_offline_user (user_id, delivered)
) ENGINE=InnoDB;
```

`idx_msg_client` 用于 ACK 路径按 `clientMessageId + senderId` 幂等查重；`idx_offline_user` 用于上线后快速捞未投递的离线消息。

#### 21.4.4 安全检查

- 默认 `bind-address = 127.0.0.1`，确认 MySQL 只监听本机。
- 阿里云安全组**绝对不要**开 3306 公网。
- root 仅 `localhost` 登录，禁止 `root@%`。
- 定时备份：`mysqldump` + crontab，输出到 `/var/backups/koukou-im/`。

### 21.5 Redis 校验与初始化

#### 21.5.1 校验是否真装

截图里 `redis` command not found 不代表 Redis 没装，常见情况是命令名错了。先校验：

```bash
redis-cli --version            # 期望输出 "redis-cli x.y.z"
sudo systemctl status redis-server
ss -ltn | grep 6379            # 期望看到 127.0.0.1:6379 LISTEN
```

如果 `redis-cli` 也找不到 / `redis-server` 服务不存在，则补装：

```bash
sudo apt update
sudo apt install -y redis-server
sudo systemctl enable --now redis-server
redis-cli ping                 # 期望 PONG
```

#### 21.5.2 设密码 + 仅本机监听

编辑 `/etc/redis/redis.conf`：

```text
bind 127.0.0.1 -::1
protected-mode yes
requirepass 换成强密码
```

```bash
sudo systemctl restart redis-server
redis-cli -a '换成强密码' ping     # 期望 PONG
```

应用侧 `.env`：

```text
REDIS_URL=redis://:换成强密码@127.0.0.1:6379/0
```

#### 21.5.3 用途约定

Redis 在 koukou-im 中承担 4 件事：

- **在线 session 索引**：`session:{userId}` 是 Set，保存该用户的所有活跃 connectionId。重启 Node 进程后内存映射会丢，Redis 是兜底；多实例部署时也靠它共享。
- **离线消息队列**：用户离线时 LPUSH 到 `offmsg:{userId}` 列表，上线后 RPOP 推送。比每次查 MySQL 快。
- **令牌黑名单 / 频控**：注销 token、防刷登录。
- **typing 状态**：5 秒 TTL，避免脏数据残留。

key 命名约定：

```text
session:{userId}        Set<connectionId>     该用户的所有活跃连接
conn:{connectionId}     Hash{userId, node}    反查归属
offmsg:{userId}         List<messageJson>     离线消息队列
typing:{convId}         String, EX 5          正在输入提示
ratelimit:login:{ip}    counter, EX 60        登录频控
revoked:{tokenId}       string, EX <ttl>      令牌黑名单
```

### 21.6 Node.js 服务进程管理

推荐 `pm2`（Node 生态默认）：

```bash
sudo npm i -g pm2
cd /opt/koukou-server
pm2 start ecosystem.config.js --env production
pm2 save
pm2 startup systemd            # 生成开机自启脚本，按提示再执行一行 sudo
```

`ecosystem.config.js` 示例：

```javascript
module.exports = {
  apps: [{
    name: 'koukou-server',
    script: 'src/index.js',
    instances: 1,                 // IM 内存维护 session，先单实例；多实例需走 Redis 共享 session
    exec_mode: 'fork',
    env_production: {
      NODE_ENV: 'production',
      PORT: 8080
      // 其他敏感字段从 .env 读，不写进这里
    },
    max_memory_restart: '512M',
    error_file: '/var/log/koukou-server/error.log',
    out_file:   '/var/log/koukou-server/out.log',
    time: true
  }]
};
```

如果不想装 pm2，可用 systemd（参考 §20.15 第二步），把 `ExecStart` 改为：

```text
ExecStart=/usr/bin/node /opt/koukou-server/src/index.js
```

### 21.7 端口、Nginx 与证书

复用 §20.9 的 Nginx 配置；本项目落地参数：

- Node.js 监听 `127.0.0.1:8080`，**不暴露公网**。
- Nginx 监听 `0.0.0.0:443` (WSS) + `0.0.0.0:80`（重定向到 443）。
- 阿里云安全组只开 22 / 80 / 443；MySQL 3306、Redis 6379、Node 8080 严禁外放。
- 域名 → ECS 公网 IP A 记录解析后，再申请 Let's Encrypt 证书（§20.10）。

### 21.8 Android 客户端配套修改

参考 §20.11，本项目落地：

- `app/build.gradle` 增加两套 `WS_URL` / `API_BASE_URL` BuildConfig 字段（debug 用本机 emulator `10.0.2.2`，release 用 `wss://api.yourdomain.com`）。
- `WebSocketManager.connect()` 在 URL 上拼 `?token=xxx`，token 从 `UserHelper` 取。
- 所有 OkHttp 请求添加 `Authorization: Bearer <token>` Header（除 `/api/login` `/api/register`）。
- 新增 `NetworkErrorBus`：连接失败 / 401 token 失效统一上报到 UI 顶部 Snackbar，避免每个 Activity 重复写。

### 21.9 当前 ECS 待办清单（按优先级）

| # | 事项 | 章节 | 阻塞上线？ |
|---|---|---|---|
| 1 | MySQL root 设密码 + 建 `koukou` 业务账号 + 执行 `init.sql` 建表 | §21.4 | **是** |
| 2 | Redis 校验是否真装；设密码 + 仅本机监听 | §21.5 | **是** |
| 3 | 部署最小 Node.js 服务，pm2 拉起，本机 `curl http://127.0.0.1:8080/health` 正常 | §21.6 | **是** |
| 4 | 域名解析到 ECS 公网 IP，申请 Let's Encrypt 证书，Nginx WSS 反代 | §20.9 / §20.10 | **是** |
| 5 | 客户端 release 构建切到 `wss://...`，真机两台对发文本消息验证 | §21.8 | **是** |
| 6 | `sudo apt upgrade` 应用 7 个未装的安全更新；如需重启则错峰执行 | — | 否 |
| 7 | 配置 `ufw`（或仅靠安全组）：放行 22/80/443，默认拒绝 | — | 否 |
| 8 | `mysqldump` + crontab 每日备份到 `/var/backups/koukou-im/` | §21.4.4 | 否 |
| 9 | pm2 监控告警（pm2-logrotate + 简单存活检测脚本） | — | 否 |

### 21.10 风险与回滚

- **风险 1：MySQL root 设密码后 Node 服务连不上**
  → 因为应用应使用 `koukou` 账号而非 root。如果误用 root，需修改 `.env` 切回 root 密码，临时跑通后再切到 `koukou`。
- **风险 2：Redis requirepass 设置后 ioredis 连接失败**
  → 检查 `REDIS_URL` 格式必须为 `redis://:<密码>@host:port/db`，密码前有冒号。
- **风险 3：Nginx 反代 WebSocket 立即断开**
  → 必须有 `proxy_http_version 1.1` + `Upgrade` / `Connection "Upgrade"` 三行（§20.9），缺一不可。
- **风险 4：pm2 实例数 > 1 后消息错乱**
  → 单进程内存维护 session 在多实例下不共享。要么保持 `instances: 1`，要么把 sessionManager 全部改成查 Redis。

## 22. 总结

Koukou 的即时通讯实现应以 `Room 本地优先 + WebSocket 实时同步 + ACK 状态确认 + 重连补偿` 为核心。发送消息时先本地落库保证 UI 立即响应，再通过 WebSocket 发送并等待 ACK；接收消息时先去重落库，再更新会话和未读数。这样既能保证交互流畅，也能为离线同步、失败重试、通知和群聊扩展打好基础。

落地到本项目：服务端栈选定 **Node.js + Express + ws + MySQL 8 + Redis**，部署在阿里云 ECS Ubuntu 22.04 上，由 pm2 守护进程，Nginx 终止 SSL 并反代 WSS。当前 ECS 环境已就绪 90%，只剩 §21.9 清单中的 5 项必做事项（重点是 MySQL/Redis 鉴权初始化）即可上线 MVP。
