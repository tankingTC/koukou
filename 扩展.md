# Koukou 扩展功能设计文档

## 1. 扩展目标

Koukou 当前已经具备登录注册、联系人、会话、单聊、设置中心、多主题背景、沉浸动效和本地数据存储等基础能力。后续扩展应围绕以下目标推进：

- 提升即时通讯完整度。
- 增强好友关系和隐私控制。
- 完善主题视觉和页面统一适配。
- 提高消息可靠性和离线体验。
- 增加本地安全、通知和存储管理能力。
- 保持 Android 原生 Java + XML、MVVM、Repository、Room、DataStore、WebSocket 的现有架构一致性。

## 2. 推荐优先级路线图

### 2.1 第一阶段：通讯基础补强

#### 好友验证申请

当前添加好友流程偏直接，建议扩展为申请制。

功能点：

- 发送好友申请。
- 申请附加验证消息。
- 申请状态：待处理、已同意、已拒绝、已过期。
- 好友申请列表页。
- 同意后自动建立双向好友关系。
- 拒绝后保留申请记录。

建议新增数据结构：

- `FriendRequestEntity`
  - `requestId`
  - `fromUserId`
  - `toUserId`
  - `message`
  - `status`
  - `createdAt`
  - `updatedAt`

建议新增页面：

- 好友申请列表页。
- 申请详情弹窗。
- 添加好友验证消息输入弹窗。

#### 消息发送状态增强

当前消息已有基础发送状态，后续可以增强为更完整的消息生命周期。

功能点：

- 发送中。
- 已发送。
- 已送达。
- 已读。
- 发送失败。
- 点击失败消息重新发送。
- WebSocket ACK 回执。

建议扩展：

- `MessageEntity.status` 统一枚举值。
- `MessageRepository` 支持重发。
- `WebSocketManager` 支持 ACK 消息分发。

#### WebSocket 重连机制

当前 WebSocket 可用，但建议补强稳定性。

功能点：

- 断线自动重连。
- 指数退避重连。
- 网络恢复后自动连接。
- 心跳 ping/pong。
- 连接状态 LiveData。
- 页面显示连接中/已断开提示。

## 3. 第二阶段：聊天体验扩展

### 3.1 群聊功能

功能点：

- 创建群聊。
- 群成员管理。
- 群名称和群头像。
- 群消息收发。
- 群消息会话摘要。
- 群成员昵称展示。

建议新增数据结构：

- `GroupEntity`
- `GroupMemberEntity`

建议调整：

- `ConversationEntity.chatType` 支持 `single` 和 `group`。
- `MessageEntity.chatType` 支持群聊。
- `MessageRepository` 根据 chatType 路由消息。

### 3.2 聊天内容增强

功能点：

- 表情面板。
- 常用 emoji。
- 图片预览。
- 视频消息预览。
- 文件消息。
- 语音消息占位。
- 长按消息菜单。
- 复制、删除、撤回、转发。

优先建议：

1. 图片点击大图预览。
2. 长按消息复制/删除。
3. 发送失败重试。
4. 表情面板。

### 3.3 会话列表增强

功能点：

- 会话置顶。
- 会话免打扰。
- 删除会话确认。
- 清空未读数。
- 搜索会话。
- 草稿显示。

建议扩展 `ConversationEntity`：

- `isPinned`
- `isMuted`
- `draft`
- `lastReadAt`

## 4. 第三阶段：通知与系统能力

### 4.1 本地通知

功能点：

- App 后台收到消息后弹通知。
- 通知点击进入对应聊天页。
- 支持通知开关。
- 支持消息预览开关。
- 支持免打扰模式。
- 支持声音、震动配置。

当前设置中心已有相关开关，后续可以接入实际通知逻辑。

实现建议：

- 使用 `NotificationManager`。
- Android 13+ 请求通知权限。
- 按会话 ID 设置 PendingIntent。
- 结合 `SettingsRepository` 判断是否展示通知。

### 4.2 后台消息处理

功能点：

- App 退到后台仍保持必要连接。
- 收到消息写入 Room。
- 更新会话未读数。
- 触发本地通知。

注意：

- Android 后台限制较多。
- 可先实现前台/最近任务内稳定收取。
- 后续再考虑前台服务或服务端推送。

## 5. 第四阶段：隐私与安全

### 5.1 本地密码保护

当前设置中已有本地密码相关字段，可扩展实际锁屏。

功能点：

- 开启本地密码。
- 设置 4-6 位 PIN。
- App 回到前台时要求解锁。
- 支持修改密码。
- 支持关闭密码。

建议新增：

- `LockActivity`
- `LocalPasscodeManager`

安全建议：

- 不明文存储密码。
- 使用哈希摘要存储。
- 可结合 Android Keystore 进一步增强。

### 5.2 黑名单能力完善

当前设置状态中已有黑名单集合，可扩展为完整功能。

功能点：

- 加入黑名单。
- 移出黑名单。
- 黑名单列表展示。
- 被拉黑用户消息拦截。
- 被拉黑用户无法添加好友。

建议：

- 从 `SettingsState.blacklistUserIds` 过渡到 Room 表。
- 新增 `BlacklistEntity` 方便查询和扩展。

### 5.3 隐私设置增强

功能点：

- 是否允许通过扣扣号搜索。
- 好友验证方式。
- 是否显示在线状态。
- 是否允许陌生人消息。
- 是否显示消息已读。

## 6. 第五阶段：主题系统扩展

### 6.1 已有主题

当前已支持：

- 蝴蝶流光。
- 全息晶尘。
- 粉色兔兔。
- 电子科幻。
- 代码雨。
- 极简暗调。
- 极简白色。
- 互动雨滴。

#### 互动雨滴主题（已实现，v3 自然流动）

视觉方向：

- 暗夜玻璃幕背景，蓝色冷光氛围。
- 水滴主体始终保持"珠状"圆润观感；运动时沿运动方向极轻微拉伸（≤1.14x）避免之前过于僵硬的胶囊形态。
- 移动水滴呈"小彗星"：头部圆珠 + 沿 −速度方向几个递减的残留珠，自然渐隐成水迹。
- 边缘暗折射环 + 主体左上亮渐变 + 左上尖锐镜面高光 + 右下次高光 + 底部 Fresnel 亮月牙。
- 重力感应驱动水滴流动；水滴流动方向（含彗星尾、残留水迹的延伸方向）严格沿重力方向自适应，倾斜手机可改变水滴行进与水迹朝向。
- 触摸涟漪、滑动擦拭，营造真实玻璃触感。
- 初始三层静态珠铺设：~140 微小冷凝点 + ~48 中等珠 + ~16 较大珠，构成自然密度。

技术要点：

- 入口：设置 → 外观与显示 → 聊天背景 → 互动雨滴（`chatBackground = "raindrop"`）。
- 核心 View：`com.example.koukou.widget.RaindropFxView`。
- 背景：`drawable/bg_scene_raindrop.xml`。
- 接入：`AppearanceManager.applyBackgroundEffects` 根据 `chatBackground == "raindrop"` 与 `immersiveEffectsEnabled` 启停。
- 物理：独立 `HandlerThread("raindrop-physics")`，固定 16ms 时间步，渲染主线程用 `postInvalidateOnAnimation` 拉动。
- 数据结构：固定大小 `DropParticle[] pool`，复用对象池避免 GC；保留 `Streak` 类供后续扩展，但 v3 中不再主动发射。
- 传感器：`Sensor.TYPE_GRAVITY`（fallback 加速度计），低通滤波 α≈0.18；动态水滴的彗星尾方向直接由实时 `(p.vx, p.vy)` 决定，因此跟随手机倾斜自动改变。
- 碰撞：屏幕网格哈希（cell≈40dp，9 格邻域扫描），融合时 R=∛(R₁³+R₂³)、V=(m₁v₁+m₂v₂)/(m₁+m₂)。
- 留痕：动态水滴只要 `speed²` 超阈值且半径足够大，就按速度比例（0.45~0.95 概率/帧）沿 `−dir` 方向以 0.5~1.2r 的偏移生成微小静态附着珠；自身按质量守恒微缩，形成由珠粒组成的水迹。
- 移动水滴渲染：按速度区间画 1~3 个递减半径（×0.78, ×0.56, ×0.34）、递减 alpha（200, 155, 110）的残留珠在 −速度方向上，再画头部主珠（沿运动方向 ≤1.14x 拉伸）。
- 静态水滴渲染：完全圆润、无拉伸、`alpha=220`，保持珠状质感。
- 渲染：一次性烤制 128×128 的"理想水滴贴图"（`buildDropAtlas`，含暗外环 + 主体渐变 + 高光 + 月牙），每帧每水滴 1~4 次 `drawBitmap(src=null, dst=RectF)` 缩放绘制，彻底消除每帧 `RadialGradient` 分配开销。
- 关闭 `LAYER_TYPE_SOFTWARE`：本 View 不使用 `setShadowLayer`，保持硬件加速路径。
- 交互：Activity 在 `dispatchTouchEvent` 调用 `RaindropFxView.dispatchToVisible` 转发，不影响普通 UI 操作；DOWN 生成涟漪并圆形擦拭，MOVE 分段擦拭并把附近水滴推开。
- 性能软上限：`MAX_ACTIVE = 110`、`MAX_STATIC = 360`，溢出时按"体积 × 存活时长"打分淘汰。
- 主题 `lightPalette` 跟随当前主题模式自动调色，重新生成贴图。

挂载范围（含 `raindrop_view` 节点）：

- `activity_chat.xml`
- `activity_settings_detail.xml`
- `activity_login.xml`
- `activity_register.xml`
- `activity_friend_profile.xml`
- `activity_version_info.xml`
- `fragment_settings.xml`
- `fragment_conversations.xml`
- `fragment_contacts.xml`

转发触摸的 Activity：`MainActivity`、`ChatActivity`、`SettingsDetailActivity`、`LoginActivity`、`RegisterActivity`。

状态：已实现并通过 `assembleDebug` 构建验证。

### 6.2 后续主题建议

#### 海洋气泡

视觉方向：

- 蓝绿色渐变。
- 半透明气泡。
- 水波纹光影。
- 柔和漂浮粒子。

适合页面：

- 聊天背景。
- 登录页。
- 设置页。

#### 暖阳橘梦

视觉方向：

- 奶油橘、浅黄、暖白。
- 日落光斑。
- 柔和卡片阴影。

适合做浅色主题。

#### 暗夜霓虹

视觉方向：

- 深紫黑背景。
- 霓虹蓝紫边框。
- 呼吸光效。
- 适合夜间使用。

#### 樱花微风

视觉方向：

- 粉白渐变。
- 樱花瓣飘落。
- 低频动画，避免卡顿。

### 6.3 主题系统优化方向

功能点：

- 主题预览卡片。
- 切换主题前实时预览。
- 每个主题独立配置：背景、卡片、文字、按钮、导航栏、气泡。
- 统一主题 token 管理。
- 降低页面中硬编码颜色数量。

建议新增：

- `ThemePalette` 类。
- `ThemeStyleResolver` 工具类。
- 将颜色、背景资源、动效模式集中管理。

## 7. 第六阶段：数据与架构完善

### 7.1 Room 显式 Migration

当前数据库使用 `fallbackToDestructiveMigration`，开发阶段方便，但数据会在版本变化时被清空。

建议：

- 为后续版本添加显式 Migration。
- 每次新增表或字段都写迁移逻辑。
- 移除 destructive migration 或只在 debug 使用。

### 7.2 Repository 职责细化

建议将部分逻辑拆分：

- `MessageRepository`
  - 消息本地存储。
  - 消息发送。
  - 消息接收。
  - 状态回执。

- `ConversationRepository`
  - 会话摘要维护。
  - 未读数维护。
  - 会话排序。

- `FriendRepository`
  - 好友关系。
  - 好友申请。
  - 黑名单判断。

### 7.3 统一错误处理

建议新增：

- `Result<T>` 或 `Resource<T>` 包装类。
- 统一 Loading、Success、Error 状态。
- ViewModel 对外暴露明确 UI 状态。

## 8. 第七阶段：用户体验增强

### 8.1 搜索功能

功能点：

- 搜索联系人。
- 搜索会话。
- 搜索聊天记录。
- 搜索设置项。

### 8.2 用户资料增强

功能点：

- 编辑头像。
- 编辑昵称。
- 编辑签名。
- 好友资料展示共同信息。
- 个性背景图。

### 8.3 动画体验

功能点：

- 页面转场统一。
- 列表项轻量入场。
- 主题切换淡入淡出。
- 长按菜单弹性动画。

注意：

- 动画应避免无限重复过多。
- 背景绘制保持低频或按需刷新。
- 保持主题切换流畅。

## 9. 推荐实施顺序

建议按以下顺序开发：

1. 好友验证申请。
2. 消息 ACK 与失败重试。
3. WebSocket 重连和心跳。
4. 本地通知。
5. 会话置顶和免打扰。
6. 图片预览和消息长按菜单。
7. 黑名单拦截。
8. 本地密码锁。
9. 群聊基础版。
10. 主题 token 化和更多主题。

## 9.1 主题性能优化（已实现）

### 代码雨主题性能调优（CodeRainView）

切换到 `chatBackground = "matrix"` 时曾出现明显掉帧。在不改变观感的前提下做如下优化：

性能瓶颈：

- 每帧每个尾部字符都执行 `setShadowLayer/clearShadowLayer`，在软件层下相当于每帧上千次模糊计算。
- 每帧重画地面网格、灭点透视线、扫描线，几十次 `drawLine` 重复绘制。
- 热路径使用 `Color.parseColor(String)` 和按字符串键的颜色缓存查询。
- 每个字符 `String.valueOf(char)` 都会创建新字符串，频繁 GC。
- 每个 drop 都重复 `setTextSize`，但同一 layer 内的 drop textSize 实际相同。

优化措施（核心文件 `com.example.koukou.widget.CodeRainView`）：

- 头部 + 光晕字符保留 `setShadowLayer`，但每个 layer 仅设置一次；尾部字符不再设置阴影，改为只在第二个尾部字符做一次轻微偏移的亮色补绘（极低成本的伪辉光）。
- 新增一次性烤制的静态覆盖层 `staticOverlay`（Bitmap），含地面网格 + 灭点线 + 扫描线，每帧只画一次 `drawBitmap`。
- `applyPalette` 中一次性把所有热路径颜色解析为 `int`（`colHeadCore`/`colHeadGlow`/`colTrailFar`/`colTrailMid`/`colTrailNear`/`colImpact*`），渲染时直接位运算修改 alpha。
- 增加 `withAlphaInt(int color, int alpha)` 工具，纯位运算返回 ARGB，避免字符串解析与 `Color.argb` 拆解。
- 新增 `decayFar/decayMid/decayNear` 衰减查表，替换循环内的 `Math.exp`。
- 复用单字符数组 `char[] glyphBuf`，用 `drawText(char[],...)` 替换 `String.valueOf(char)`。
- 同 layer 内 textSize 一致，把 `setTextSize` 上提到 `drawDropsForLayer` 入口一次设置。
- `onDetachedFromWindow` 中回收 Bitmap。

视觉对齐：

- 观感保持与优化前基本一致：头字符仍有明显辉光、尾部仍呈衰减、地面网格透视、撞击粒子与涟漪保留。
- 在尾部字符上的"近距软发光"由廉价的偏移补绘替代，肉眼上仅在很近距离对比能略察觉差异。

帧率提升（v2 流畅化）：

- 原 `FRAME_DELAY_MS = 33L`（约 30fps）观感顿挫，改为 `postInvalidateOnAnimation()`，使绘制与显示器 vsync 对齐，目标 60fps。
- 由于 v1 性能优化已经把每帧 CPU 成本降下来，60fps 在中端设备上稳定可达。
- `MAX_PARTICLES`、`MAX_RIPPLES` 保持不变，使观感与之前完全一致。

硬件加速彻底化（v3 修复 60fps 仍卡）：

- 直接提到 60fps 后，仍保留的 `LAYER_TYPE_SOFTWARE` 让整 View 走 CPU 路径，叠加每帧约 80 头字符 × 2 次 `setShadowLayer` 的高斯模糊，blur op 数量翻倍 → 重新出现掉帧。
- 移除 `setLayerType(LAYER_TYPE_SOFTWARE, null)`：恢复硬件加速路径。
- 头部辉光：把 `setShadowLayer` 完全去掉，改用 4 方向（±x / ±y）偏移补绘 + 中心实体 = 5 次廉价 `drawText` 实现"伪辉光"，肉眼几乎一致，但硬件加速友好。
- 地面涟漪：原本 2 次 `setShadowLayer` 软发光也很贵，改为同一椭圆/弧线连画 3 道（外圈宽淡 + 主圈 + 内圈高亮弧）模拟软光晕。
- 颜色字面值预处理为 int（`0x00AFFFF9` 等），配合 `withAlphaInt` 直接位运算注入 alpha，热路径零字符串解析。
- 此后 60fps 下 CPU 端不再有 blur op，整张 View 走 GPU。

### ThemeAtmosphereView 同类优化（蝴蝶 / 晶尘 / 兔兔 / 科幻 / 极简共用）

该 View 是除 `CodeRainView` / `RaindropFxView` 之外承载所有其它主题的容器，原存在同样的瓶颈：

- `LAYER_TYPE_SOFTWARE` 整图走软件渲染；
- `FRAME_DELAY_MS = 33L` 固定 30fps；
- **stardust 模式**最严重：3 个背景光斑 + 11 个闪烁星 + 56 个粒子（每个 1~3 次 `setShadowLayer`），每帧累计 70~120 次高斯模糊。

应用同套规则修复：

- 移除 `setLayerType(LAYER_TYPE_SOFTWARE, null)`，整 View 走硬件加速。
- 帧率改 `postInvalidateOnAnimation()`：60fps + vsync 对齐。
- 抽取通用工具 `drawGlowBlob(...)`：用 3 圈同心圆（外淡 / 中 / 实体）替代 `setShadowLayer` 软发光。
- `drawStardustParticle` 三种形态全部去 shadow：
  - 圆点：外圈 2×r 大光晕 + 中圈 1.4×r + 主体 + 中心高亮 4 层叠加；
  - 菱形/棱镜：同形状描宽线作"伪光晕"+ 主体填充 + 细描边 + 中心亮点；
  - 流星线：1 条宽光晕线 + 1 条窄实体亮线。
- 背景 3 个色块和 11 颗闪烁星：统一走 `drawGlowBlob` 或外圈淡光 + 实体的 2~3 层叠加。
- 蝴蝶流光 / 极简 / 兔兔 / 电子科幻模式没有 `setShadowLayer`，本身只是被软件层拖慢，移除软件层即顺势获益。

## 9.2 主题样式 token 化（ThemePalette，已实现）

为了避免 fragment / adapter / activity 散落 `Color.parseColor("#XXXXXX")` 与 `"matrix".equals(bg) ? ... : "stardust".equals(bg) ? ...` 的多层三元，所有主题相关的颜色与背景 drawable 都集中到一份不可变的语义化调色板。

**位置**：`com.example.koukou.theme.ThemePalette`

**字段（按职责分组）**：

- 文字：`textPrimary` / `textSecondary` / `textTertiary` / `textAccent`
- 图标：`iconPrimary` / `iconAccent` / `iconMuted`
- 描边：`cardStroke`
- 背景 drawable：`bgGlassCard` / `bgListCard` / `bgSettingsRow` / `bgSettingsActionRow` / `bgPanel` / `bgBottomNav` / `bgUnreadBadge` / `bgInputBar` / `bgInputBarActive`
- Switch：`switchOffTrack` / `switchOffThumb` / `switchActiveAlpha` / `switchElevation`
- 导航：`navBadgeColor`（0 表示用默认 cyan）/ `navInactive` / `navSelected`
- 元信息：`lightPalette`（用于状态栏图标反色等）

每个主题对应一个 `public static final ThemePalette` 常量，目前覆盖 `BUTTERFLY` / `CYBER` / `RAINDROP` / `MINIMAL` / `MATRIX` / `STARDUST` / `MINIMAL_WHITE` / `BUNNY`。`ThemePalette.forState(state)` 根据 `chatBackground` 选择 palette，未知主题回退到 `BUTTERFLY`。

**业务侧统一入口**：

- `AppearanceManager.currentPalette(Context)`：在 adapter / Activity onCreate 等已有 Context 的位置使用。
- `AppearanceManager.paletteOf(SettingsState)`：在 `observeAppearance` 回调里使用，避免再读一次 LiveData。

**使用模式（迁移前后对比）**：

```java
// ❌ 旧写法（散落、重复、易错）
boolean minimalWhite = "minimal_white".equals(state.chatBackground) || "pink_bunny".equals(state.chatBackground);
boolean matrix = "matrix".equals(state.chatBackground);
boolean stardust = "stardust".equals(state.chatBackground);
binding.toolbar.setTitleTextColor(Color.parseColor(minimalWhite ? "#162131" : "#F3F6FC"));
binding.tvSignature.setTextColor(Color.parseColor(minimalWhite ? "#6A778C" : (matrix ? "#A9C8BE" : (stardust ? "#C6D4EA" : "#B9C2D5"))));

// ✅ 新写法（一行一个语义槽）
ThemePalette palette = AppearanceManager.paletteOf(state);
binding.toolbar.setTitleTextColor(palette.textPrimary);
binding.tvSignature.setTextColor(palette.textSecondary);
```

**已迁移调用点**：

- `ConversationAdapter` / `ConversationsFragment`
- `ContactAdapter` / `ContactsFragment`（包含好友申请对话框）
- `SettingsFragment` / `SettingsAdapter`（Arrow / Switch / Action 三种 holder）/ `SettingsDetailActivity`
- `MainActivity`（底部导航 + badge）
- `ChatActivity`（emoji 面板 + 输入条）

**新增主题流程**：

1. 在 `ThemePalette` 内增加一个静态工厂方法（如 `oceanBubble()`），按字段顺序填齐所有槽位。
2. 在 `forState(SettingsState)` 的 switch 中登记新的 `chatBackground` 字符串。
3. 在 `AppearanceManager.applyPageAppearance` / `resolveBackgroundResource` 等已有逻辑中也补上对应分支（场景 drawable）。
4. 业务代码不需要修改 —— 所有页面会自动按新 palette 呈现。

**新增组件槽位流程**：

1. 在 `ThemePalette` 添加新字段（如 `dialogScrim`）。
2. 在 8 个 palette 工厂方法中各补一行赋值（编译器只对 IDE 提醒，不会强制；`@DrawableRes` / `@ColorInt` 注解辅助审核）。
3. 业务代码读取 `palette.dialogScrim` 即可。

**未迁移 / 仍硬编码的部分**：

- 聊天气泡 (`MessageAdapter`)：气泡背景仍按 chatBackground 判断，主题变量较多，下一阶段统一。
- 部分 dialog 内嵌组件颜色（设置详情页内若干 AlertDialog 自定义视图）：因 dialogView 内嵌结构复杂，单独 PR。
- 登录 / 注册 / FriendProfile / VersionInfo 页面：尚未接入 palette，目前依赖 XML 自带 selector 与默认色，下一阶段补齐。

## 9.3 布局规范 token 化（已实现）

`@/d:/Android/project/koukou/android页面布局规范.txt` 中定义的 8dp 网格、组件尺寸、卡片/输入/按钮规范已抽象为 `@/d:/Android/project/koukou/app/src/main/res/values/dimens.xml` 的语义化 token。所有页面通过引用 token 保持视觉一致，新增主题/页面只需复用 token，无需再手填像素值。

**token 总览**：

- 间距：`space_xs` (4) / `space_sm` (8) / `space_md` (16) / `space_lg` (24) / `space_xl` (32) / `space_xxl` (48)
- 容器：`app_bar_height` (56) / `bottom_tab_height` (56) / `page_horizontal_padding` (16) / `page_section_spacing` (24) / `form_max_width` (560) / `content_max_width` (840)
- 卡片：`card_radius` (16) / `card_inner_padding` (16) / `card_min_height` (72) / `card_elevation` (4)
- 按钮：`button_primary_height` (48) / `button_secondary_height` (40) / `button_small_height` (32) / `button_icon_size` (48) / `button_primary_corner` (24) / `button_secondary_corner` (20)
- 输入：`input_min_height` (56) / `input_corner_radius` (12) / `input_horizontal_padding` (16)
- 列表项：`list_item_single_height` (56) / `list_item_double_height` (72) / `list_item_triple_height` (88)
- 气泡：`bubble_corner` (20) / `bubble_padding` (12)
- 头像：`avatar_sm` (32) / `avatar_md` (40) / `avatar_lg` (56) / `avatar_xl` (80)
- 弹窗：`dialog_corner` (16) / `dialog_padding` (24)

`@/d:/Android/project/koukou/app/src/main/res/values-sw600dp/dimens.xml` 提供宽屏断点（>600dp）下的 page padding / form max width 增大值，达成响应式适配。

**已迁移**：`activity_login` / `activity_register` / `activity_chat` / `activity_main` / `activity_settings_detail` / `activity_friend_profile` / `fragment_settings` / `fragment_contacts` / `dialog_add_friend` / `dialog_change_password` / `dialog_edit_profile` / `dialog_settings_choice` / `dialog_settings_confirm` / `item_msg_send` / `item_msg_receive` / `item_conversation` / `item_account_history` / `item_dialog_option` / `layout_account_history`。除装饰资产外，全项目布局内硬编码 dp 数值（10/12/14/18/20/22/28/36/40/54/58/76/78/92 等非 8dp 网格值）已全部消除。

**保留硬编码（艺术资产）**：`view_iridescent_butterfly.xml`（蝴蝶光翼装饰图层，按特定像素摆放是设计需要）。

## 9.4 登录/注册视觉重构（B+C：品牌沉浸 + 历史账号优先）

`@/d:/Android/project/koukou/app/src/main/res/layout/activity_login.xml` 在保持品牌 hero 蝴蝶的同时，引入"历史账号优先"模式：

- **品牌 hero**：从 `452dp×388dp + marginEnd=-96dp`（小屏会被裁掉）改为 `layout_constraintWidth_percent=0.62 + DimensionRatio H,1.18:1`，自动按屏幕宽度缩放，永不溢出。
- **装饰收敛**：移除 `glow_sheen_top` / `glow_sheen_bottom` 两个旋转 sheen，仅保留 `glow_halo`（且 size 从 360 缩到 320，alpha 从 0.22 到 0.18）。`view_iridescent_butterfly` 自身的光效已足够。
- **滚动包裹**：根布局内嵌 `NestedScrollView`，加 `windowSoftInputMode="adjustResize"`，键盘弹起后仍能滚到登录按钮。
- **历史账号卡片（cv_history）**：当 `UserHelper.getSavedLogins()` 非空时作为首屏入口卡片。RecyclerView 卸入式（不再下拉），每行点击直接登录（不需要再点登录按钮）。
- **"使用其他账号" 切换按钮（btn_use_other）**：浅色 outline，点击淡入 `cv_form` 切到表单输入模式。
- **表单卡片（cv_form）**：当无历史账号 / 用户主动切换时可见。圆角统一：卡片 `card_radius`(16) / 输入 `input_corner_radius`(12) / 按钮 `button_primary_corner`(24) `button_primary_height`(48)。
- **fallback 老下拉**：保留 `tilAccount` 右侧下拉图标的旧式行为（使用 `layoutHistory.cardRoot`），点击空白处可关闭。
- 旧的 `glow_sheen_top` / `glow_sheen_bottom` View 在 XML 中保留 0×0 GONE 的占位 view，用以让 `LoginActivity.observeAppearance` 中遗留的 `binding.glowSheenTop` / `binding.glowSheenBottom` 引用不致 NPE，后续可清理。

`@/d:/Android/project/koukou/app/src/main/java/com/example/koukou/ui/login/LoginActivity.java` 增加：

- `applyEntryMode()`：根据 `savedLogins` 是否为空切换首屏 = 历史卡片 / 表单卡片。
- `showFormMode()`：动画切换到表单模式。
- `HistoryAdapter.OnItemClickListener.onItemClick`：点击历史项直接 `viewModel.login(account, password)`，不再走"填表单 + 用户再点登录"两步。
- `deleteAccount`：当历史清空时自动切到表单模式。

注册页同步 token 化、`hero` 改 percent、加 `NestedScrollView`，但保持单一表单卡，未引入历史模式。

### 9.4.1 登录/注册接入 ThemePalette + 提示体验升级

之前登录/注册页所有静态文字都用 `@color/butterfly_*`，切到浅色主题（`minimal_white` / `pink_bunny`）会出现"白底白字"对比度严重不足。本轮全面接入 `@/d:/Android/project/koukou/app/src/main/java/com/example/koukou/theme/ThemePalette.java`：

- **`LoginActivity.applyPalette()` / `RegisterActivity.applyPalette()`**（在 `observeAppearance` 中调用）：
  - 标题 / 副标题 / 描述文字 → `palette.textPrimary` / `textSecondary`
  - logo / btnBack 容器 → `palette.bgPanel`
  - 卡片 stroke → `palette.cardStroke`，卡片内部背景 → `palette.bgGlassCard`
  - `TextInputLayout`：`boxStrokeColor` / `hintTextColor` / `defaultHintTextColor` / `helperTextColor` 按 palette；`boxBackgroundColor` 设为 `Color.TRANSPARENT`，让 glass card 背景透出，避免在浅色主题下出现深色块。
  - `EditText` 内部 `textColor` / `hintTextColor` 同步 palette。
  - `applyTextColors(view, palette)` 递归遍历卡片内所有 `TextView`，按字号 (sp) 自动归类到 primary/secondary/tertiary，避免每个 TextView 单独写 setTextColor。
- **错误提示改为内联**：原本登录/注册校验失败用 `Toast.makeText(...)`，现在统一改用 `TextInputLayout.setError("...")` + `requestFocus()`，并在 `EditText.addTextChangedListener` 中输入新内容时自动清错（`attachErrorClearer` 工具方法）。
- **Snackbar 替代 Toast**：`showTip()` 改用 `Snackbar.make()`，背景色 `palette.lightPalette ? 0xEEFFFFFF : 0xCC152443`、文字色 `palette.textPrimary`，与主题一致。
- **清理 `glow_sheen_bottom` 冗余调用**：登录页 `observeAppearance` 中 `binding.glowSheenBottom.setVisibility(GONE)` / `IridescenceAnimator.stopEffects(binding.glowSheenBottom)` 已删除（XML 中该 view 是 0×0 GONE stub，调用是 no-op）。XML 占位 view 仍保留以兼容 ViewBinding 字段不破坏。

新增主题需要重写登录/注册页样式时，**只需在新 palette 工厂里给已有字段赋值**，不需要再到 LoginActivity / RegisterActivity 改一行代码。

## 9.4.2 全项目 token 化二轮收尾（规范 v2.0 对齐）

配合 `@/d:/Android/project/koukou/android页面布局规范.txt` v2.0 发布，对全项目做一次彻底收尾，消除所有剩余的"裸 dp / 硬编码 #RRGGBB"。

**新增 dimens token（`@/d:/Android/project/koukou/app/src/main/res/values/dimens.xml`）**：

- 图标：`icon_sm` (16) / `icon_md` (24) / `icon_lg` (32)
- 角标：`badge_min_size` (20) / `badge_padding_horizontal` (6) / `badge_padding_vertical` (2)
- 杂项：`stroke_width` (1) / `swipe_action_width` (80) / `msg_row_padding_v` (4)

**新增 named colors（`@/d:/Android/project/koukou/app/src/main/res/values/colors.xml`）**，作为 XML 初始展示态：

- `butterfly_stroke_accent` `#6C96DFFF`（主要卡片 stroke）
- `butterfly_stroke_muted` `#30F3F6FC`（弱化卡片 stroke）
- `butterfly_input_bg` `#4D141A2E`（输入框深色底）
- `butterfly_input_bg_strong` `#9C152443`（登录页表单输入框底）
- `butterfly_swipe_delete_bg` `#A8522A42`（侧滑删除按钮底）
- `butterfly_on_swipe_delete` `#FFF2F6FF`（侧滑删除文字）

**迁移文件清单**：

- `fragment_settings.xml` / `activity_login.xml` / `activity_register.xml` / `layout_account_history.xml`：`app:strokeColor` / `app:strokeWidth` / `app:boxBackgroundColor` 从硬编码 hex 改为 `@color/butterfly_stroke_accent` 等命名色 + `@dimen/stroke_width`。
- `item_conversation.xml`：未读 badge 的 `minWidth=22dp` / `padding=6dp,2dp` → badge token。
- `item_dialog_option.xml`：选中指示 18dp icon → `@dimen/icon_sm`。
- `item_account_history.xml`：22dp icon → `icon_sm`；3dp margin → `space_xs`；82dp 侧滑宽 → `swipe_action_width`；删除按钮底色/文字色 → `butterfly_swipe_delete_bg` / `butterfly_on_swipe_delete`；16dp padding → `space_md`；补 `contentDescription="@null"`。
- `item_msg_send.xml` / `item_msg_receive.xml`：外部 6dp padding → `msg_row_padding_v`；4dp → `space_xs`；48dp minHeight → `button_primary_height`。
- `item_contact.xml`：4dp → `space_xs`。
- `item_settings_title.xml`：6dp / 2dp → `space_sm` / `space_xs`。
- `item_settings_switch.xml` / `item_settings_arrow.xml`：24dp icon → `icon_md`；3dp margin → `space_xs`。

**规范文档更新**：`android页面布局规范.txt` v2.0 发布，铁律 3 澄清"8dp 网格只对元素间距生效，组件内部构件（圆角/icon/badge）允许非网格但必须 token 化"，消除自相矛盾；新增 §1.10 图标 / §1.11 Badge / §1.12 杂项三张 token 表。

编译验证：`gradlew assembleDebug` = **BUILD SUCCESSFUL**。

## 9.5 主题特效性能公约（新增主题必读）

凡是在 koukou 中新增"全屏主题特效 View"，必须遵守以下铁律，避免再次走回卡顿老路：

铁律 1：**永远不要 `setLayerType(LAYER_TYPE_SOFTWARE, null)`**。

- 软件层会让整 View 每帧走 CPU 路径，是所有主题卡顿的根因。
- 若想做发光、模糊、合成等需要软件层的效果，请用几何叠加（多层同心 / 多次偏移 drawText）来近似，不要切到软件层。

铁律 2：**永远不要 `setShadowLayer`**。

- `setShadowLayer` 触发 gaussian blur op，在硬件加速下也比普通 draw 慢一个数量级，软件层下更夸张。
- 替代方案：
  - 圆形软光晕 → `drawGlowBlob(cx, cy, r, color, glowAlpha, coreAlpha)` 风格的 2~3 圈同心圆叠加；
  - 文字辉光 → 4 方向（±x / ±y）偏移补绘 + 中心实体 = 5 次廉价 `drawText`；
  - 路径辉光 → 同形状描宽线（低 alpha）打底 + 主体路径在上；
  - 椭圆/线段辉光 → 外圈宽淡 + 主圈/线段在上。

铁律 3：**帧率用 `postInvalidateOnAnimation()`，不要 `postInvalidateDelayed(N)`**。

- `postInvalidateOnAnimation` 与 Choreographer / vsync 对齐，自动跟随设备刷新率（60 / 90 / 120）。
- 固定 `postInvalidateDelayed(33L)` 会与 vsync 错位，造成观感顿挫。

铁律 4：**热路径里不要 `Color.parseColor(String)`**。

- 解析放在 `applyPalette()` 或字段初始化时一次完成，结果以 `int` 形式缓存（推荐 `0x00RRGGBB` 仅 RGB），配合 `withAlphaInt(int color, int alpha)` 做位运算注入 alpha。
- 单纯走 `HashMap<String,Integer>` 的查表虽不慢但仍劣于直接位运算。

铁律 5：**热路径里不要 `String.valueOf(char)` / `new XxxGradient(...)` / `new RectF(...)`**。

- 文字：用复用的 `char[1] buffer` + `drawText(char[],...)`。
- 渐变：把渐变烤进 `Bitmap` atlas 一次性生成，每帧只 `drawBitmap(src=null, dst=RectF)` 缩放绘制。
- 临时几何：用类字段级别的 `RectF`/`Path` 复用，每帧 `reset()` + 重新填充。

铁律 6：**重复几何（背景网格、扫描线、灭点透视等）烤进 Bitmap 缓存**。

- `onSizeChanged` 时重建一次 Bitmap，每帧只 `drawBitmap` 一次。
- `onDetachedFromWindow` 中 `recycle()` 防止泄漏。

铁律 7：**密度上限保留软上限 + 老化淘汰**。

- 静态对象列表（如雨滴、星尘）设硬上限（`MAX_*` 常量），溢出按"体积 × 存活时长"打分淘汰。
- 对象池（`DropParticle[] pool`）复用，避免 GC 抖动。

铁律 8：**物理与渲染解耦**。

- 重物理仿真（雨滴碰撞、网格哈希）放独立 `HandlerThread`，主线程只做 `postInvalidateOnAnimation` 触发 `onDraw`。
- 主线程读写共享数据时 `synchronized` 块控制临界区，必要时拷贝快照避免长锁。

参考实现：

- 高吞吐文字粒子：见 `CodeRainView`（4 方向伪辉光 + 静态 overlay bitmap + decay 查表）。
- 高拟真水滴：见 `RaindropFxView`（贴图 atlas + 彗星尾 + 物理线程）。
- 多模式背景：见 `ThemeAtmosphereView`（`drawGlowBlob` + 同心圈光晕替代 shadowLayer）。

## 10. 风险与注意事项

- 主题动效要控制绘制频率，避免再次造成切换卡顿。
- WebSocket 重连要避免无限快速重试。
- 数据库升级要配合 Migration，避免用户数据丢失。
- 通知权限要兼容 Android 13+。
- 本地密码不能明文保存。
- 群聊会影响消息、会话、联系人多个模块，建议最后做。

## 11. 总结

Koukou 后续最值得优先扩展的是好友验证、消息可靠性、WebSocket 稳定性和通知能力。这些能力能显著提升即时通讯完整度。主题系统可以继续作为特色方向扩展，但建议逐步抽象成统一主题 token，减少每个页面单独判断主题带来的维护成本。
